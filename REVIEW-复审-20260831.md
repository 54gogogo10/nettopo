# NetTopo 代码复审（R5 · 自动修改复核）— 2026-08-31

## 1. 范围与结论

- **复审对象**：R4 审查修复提交 `e7c30a1` 及其后续集成测试提交 `dc0f589`、`81f7071`、版本提交 `44e7fea`/`bb53a87`/`9e2f02c`（即 R4 报告落地后的全部自动修改）。
- **结论**：R4 的 3 个 Medium、11 个 Low 修复**全部核实到位且未引入回归**；本次复审另发现 **1 处修复自身的新缺陷（保留名正则多级扩展名缺口，Low）**并已修复，同时收尾了 R4 遗留的 **L-10（区域/聚合组层每帧全量 DOM 重建，Low·性能）**。单元测试 805 → **809 全绿**，e2e 34 段两遍全绿，smoke-backup/center/monitor 全过（smoke-shell 见 §4 环境备注）。

## 2. R4 修复逐项复核（全部通过）

| R4 项 | 复核结果 |
| --- | --- |
| M-1 区域拖动 moved 口径 | 改为 `rx0/ry0` 固定起点累计位移，与 `_startDrag` 一致；e2e「区域慢速拖动+撤销」覆盖 |
| M-2 CDP detail 同行 Port ID | `Interface:` 值内二次提取 `[,，] Port ID (outgoing port):`，实测格式正确 |
| M-3 段头多邻居继承 localIf | `segLocal` 继承；`Local Intf:` 块头会重置 segLocal，段内口径正确 |
| L-1 跳板指纹归属 | `_runBackupOwn` 改用 `info.host`（fingerprint 事件自带目标主机） |
| L-2 SNMP 抢答 | `snmpResponseMeta` 校验 request-id + community，不匹配丢弃续等；snmpWalk 经 snmpGetNext 走同一校验，无旁路 |
| L-3 合规回溯防护 | 主进程 `compileComplianceRules` 与渲染层 `cleanComplianceRules` 同加嵌套量词启发式 + 单行 10000 截断；默认规则均不误伤（逐条验证） |
| L-4 保留设备名 | 实现到位，但正则本身有缺口 → 本次修复（见 §3.1） |
| L-5 transport input 词序 | `transport input\s+(?:ssh\s+)?(?:telnet|all)`，`ssh telnet`/`all`/单 `telnet` 均命中、单 `ssh` 不误报；三套规则包同步 |
| L-6/L-7/L-8 解析支持面 | 思科 LLDP 标准表头（local+intf+device id 组合）、H3C verbose 段头/`Neighbors' ...` 键名、CDP 表格续行拼接（限一次）均到位；三路解析按条数择优的调度不受放宽影响 |
| L-9 separateOverlaps 大图 | >2500 跳过，与 simulate 同口径 |
| L-11 暗色描边 | `:where([data-theme="dark"])` 降为 (0,2,0)，状态规则 (0,3,0) 恒胜、基础 (0,2,0) 仍被源码顺序覆盖；全表核对无其他 (0,2,0) `.shape` 规则冲突；e2e 两主题描边回归在位 |

测试基建：`gen-e2e.js` 再生挂具与 index.html **零漂移**（本次实测）；e2e 用例经合成 pointer 事件绕开 pointer state 漂移属测试侧技巧，不影响产品代码。

## 3. 本次新发现与修复

### 3.1 保留设备名正则漏拦多级扩展名（Low · R4/L-4 修复自身的缺口）

- **位置**：monitor.js / shell.js / config-backup.js 三处同款正则 `(\.[^.]*)?$`。
- **问题**：Windows 的保留判定看**首个圆点前的词干**，`nul.a.b`、`com1.tar.gz`（两段以上扩展名）不匹配 `\.[^.]*$` → 目录/文件创建仍静默失败，日志与配置备份丢失面未完全闭合。
- **修复**：改为 `(?:\.|$)` 词干判定；新增单测 3 项（nul.a.b / com1.tar.gz / lpt1x.log 不误伤）+ config-backup 真实落盘回归 1 项（`_nul.a.b/1.2.3.4/` 目录可写）。

### 3.2 L-10 收尾：区域层/聚合组层每帧全量 DOM 重建（Low · 性能）

- **位置**：render.js `_buildRegions`/`_buildGroups`（`update()` 在节点/文本/区域拖拽与缩放中每帧调用）。
- **修复**：两层各加重建签名——区域（几何+名称+颜色+缩放+框内设备数）、聚合组（几何+名称+颜色+成员数+缩放）；签名未变跳过 `innerHTML=''` 全量重建。`setData` 清空 regionLayer 处同步重置签名防「层已空但吃缓存」。`regionEls` 只写不读，无旧引用风险；节点拖拽不再触发重建，跨框/改缩放/改名仍即时刷新。
- **验证**：e2e 34 段两遍全绿（含区域慢速拖动+撤销、网段分析、主题描边）。

## 4. 环境备注（非代码问题）

- smoke-shell 在本机失败 8 项（xterm 渲染/SSH 会话类）：**干净基线 9e2f02c 同样失败同样 8 项**（stash 对照实测），且 2323/9333 端口无占用、无残留 Electron 进程——属本机 CDP 检测 xterm 输出的环境问题，非本次或 R4 改动引入。smoke-backup / smoke-center / smoke-monitor 全部通过。
- e2e 段数口径：32 行 `: OK` + 2 行 `✓`（导出无异常、无控制台错误）= README 所称 34 段，一致。

## 5. R4 Info 项状态（未变，随迭代处理）

subnetCalc 越界统一、sanitizeGraph aMask/bMask 钳制、applyNeighbors fresh id、接口总表勾二层清 IP 口径、合规 UI 自伤正则、TOFU 文档化、visio.js 死代码确认、bump-version 一致性校验等仍为 Info 待办，本次未动。
