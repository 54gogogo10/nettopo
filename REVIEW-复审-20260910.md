# NetTopo 全面代码审查与修复（R7）— 2026-09-10

## 1. 范围与结论

- **审查方式**：7 路并行专项审查（app 业务 / util+model+layout / 渲染与导出链 / Shell 与独立窗口 / 监控+SNMP+诊断 / 内置网络服务与备份库 / 主进程+AI+更新器），全量通读约 2.6 万行源码；每项发现均经人工核实代码证据后修复（含 2 项经核实为误报不予修改，见 §5）。
- **结论**：确认并修复 **56 项**（High 3 / Medium 22 / Low 31），另修复过程中连带发现并处理 1 项（FTP 活动数据连接泄漏，并入 M1）。
- **验证**：单元测试 1469 → **1509 全绿**（新增 40 项回归）；e2e 34 段两遍全绿（无控制台错误）。

## 2. High 级修复（3 项）

| # | 位置 | 问题与修复 |
| --- | --- | --- |
| H1 | electron-main.js | 会话录制 5 个 IPC handler（record-start/append/list/read/delete）引用未定义的 `fs`（模块顶层未 require，各函数局部 require 又未覆盖该批 handler）：`record-start` 恒报 "fs is not defined"、`record-list` 恒空——录制功能整体损坏。顶层统一引入 `fs` |
| H2 | js/ai-llm.js | `listModels()` 的 `fail()` 闭包引用响应回调内的局部 `idleTimer`：DNS 失败/连接拒绝/超时/响应中断等所有网络错误路径先抛 `ReferenceError` 且 Promise 永不落定——内网填错地址端口（ECONNREFUSED 高频场景）必现「获取模型列表」按钮永久等待，异常以 uncaughtException 落 main-crash.log。`idleTimer` 提升到 executor 作用域（与 `_post` 的正确写法同口径） |
| H3 | js/app.js restoreGraph() | 多图纸模式下活动页为空时刷新：`!d.nodes.length` 直接放弃恢复（顶层 nodes 是活动页本体，可为空数组），`state.sheets` 保持 `[]`，其后首次 saveGraph（滚轮缩放的 500ms 节流保存即触发）以空 sheets 覆盖 localStorage——**其他页数据永久丢失**。守卫改为「顶层空但 sheets 存在非空页时仍走恢复」 |

## 3. Medium 级修复（22 项）

**内置网络服务（输入不可信，主进程内执行）**
- M1 svc-ftp.js STOR：服务端主动断开（421 空闲超时/QUIT/stop() 的 `destroy()`）产生的 `close` 事件同样 `hadErr=false`，与客户端正常 FIN 无法区分——**半截文件被 rename 成品入库、累加统计并广播「已接收」**（违背 TFTP 侧已修复的「rename 失败不确认」语义）。close/end 回调补 `this.closed` 拦截并清理 `.part`。连带发现：已接受的数据连接不归 `pasvPending` 管（accept 后即置 null），`_closePasv()` 不销毁它——服务端断开时数据连接**半开泄漏**、STOR 收尾回调永不触发；`_openData()` 现登记 `_activeData` 统一回收
- M2 svc-ftp.js PASV：数据 server `listening` 成功后唯一的 error 监听被移除且未补挂——accept 系统调用失败（EMFILE/ENOBUFS 等，局域网任意主机可触发）产生无监听 error 事件**直接崩掉主进程**。补持久监听并 `_closePasv()`
- M3 svc-tftp.js WRQ：收尾窗口（`ws.end` → `finish`）内对端 1s 重传定时器到期的最终块重传走「重传补 ACK」分支被**提前 ACK**——若随后 rename 失败（目标被占用），设备已凭 ACK 认定推送成功而忽略 ERROR，配置实际丢失；且 `finished` 在 rename 前置位，写流出错时 abort 跳过 `.part` 清理。finishing 窗口静默等 finish 裁决；`finished` 移到 rename 成功后置位
- M4 svc-syslog.js TCP：连接关闭不 flush 残留缓冲——不少设备发完最后一条即 close 且不带尾部 `\n`（RFC 6587 未强制），**最后一条日志高频丢失**。close 回调补 flush
- M5 svc-syslog.js：落盘目录名由消息体自报 HOST 决定且无上限——局域网攻击者在限速内每条换一个伪造 HOST 可持续新建目录（目录项/inode 耗尽 DoS）；`_cleanupOld` 删文件后空目录永久残留且同步遍历越来越重。`MAX_HOST_DIRS=1024` 封顶（超限新主机只进环形缓冲）；清理后 rmdir 空目录（有活跃写流的主机跳过）
- M6 net-services.js `applyConfig` 无并发互斥：启动恢复 vs 面板保存、快速连续保存会在 `await stop` 与 `new` 替换实例之间交错——被甩掉的实例已 start 成功则监听端口**永久泄漏**，applied 标记被覆盖为 null，面板显示未运行、重试恒 EADDRINUSE 直到重启。promise 链串行化

**监控 / SNMP / Shell（不可信设备端输入）**
- M7 svc-trap.js v3 Trap 认证绕过：`parseV3Message` 是否做认证校验由**包内自报 flags** 决定，伪造 flags=0 的 noAuth 包携带 linkDown 等可原样通过明文解析直入告警环形缓冲并 emit——v3 认证形同虚设（退化回 v2c）。`_ingestV3` 按本端配置用户的安全级别强制：要求认证（auth/authPriv）的用户拒收未认证包；显式配置 noAuth 空口令的用户照常接收（等价 v2c 语义）
- M8 monitor.js 独立备份 `lineBuf` 无上限：恶意 SSH/Telnet 服务端持续灌不含换行的输出（就绪等待窗最长 15s + 命令执行窗），行缓冲按网络速率无界增长（对比 `_onOutput` 有 256K 强制断行）。同口径补断行（断行段同样过打码与 8MB 预算）
- M9 monitor.js 独立备份尾部半行未过 `_maskSecrets`：恶意设备把密码回显留在无换行结尾的尾段，明文密码随备份内容持久化。补打码
- M10 regex-lab.js `_curIndex` 为实例级字段而 RegexLab 在 MonitorManager 内单实例共享：并发 run 时超时处决的元凶定位被后启动批次覆盖——**无辜的告警模式被永久拉黑**（该关键字告警从此静默失效）。当前索引闭包局部化
- M11 shell.js `trustFingerprint` 按 host 放行全部待确认握手且不区分归属：UI 首连弹「确认主机指纹」等待人工决策期间，同主机的监控/一次性采集连接自动信任会把 **UI 会话的握手一并放行**（用户还没点「信任」连接已建立，点「取消」已无效果），反向的用户拒绝也会误杀后台采集。`trustFingerprint` 增加 owner 过滤：后台（monitor）自动信任只放行 monitor 握手，UI 人工确认保持放行全部（TOFU 原语义）
- M12 shell.js SSH `resize` 不回写 `o.cols/o.rows`：reconnect 以首次 connect 的 base 尺寸（80x24）开新 shell 通道，用户放大过窗口后重连即与本地 xterm 尺寸错位（折行错乱、全屏程序花屏）。与 `_telnet` 同口径回写
- M13 shell-ui.js：群发条（setCastMode）与 AI 结果条（showAiResult/hideAiResult）显隐不重算终端 fit（同类场景 setAiBar/setSftpOpen/setFontSize 均有）——in-flow 子元素改变终端可用高度，`.sh-terms overflow:hidden` 把底部 2-3 行（含提示符）裁掉。提取 `refitActive()` 共用

**主进程 / 更新器**
- M14 electron-main.js `update:download` 直接采信渲染层回传的 assets：被攻破的渲染层可指定任意 https 地址的 exe 并附自制 `.sha256` 清单——校验必过，`apply()` 换入即任意代码执行；且资产版本不经 compareVersion 复核（降级攻击）。资产一律以主进程实时 `check()` 拉取的 release 为准
- M15 electron-main.js `ai:analyze` 无并发限制：客户端按次新建令 AiClient 单飞失效，`aiActiveClient` 被覆盖后 `ai:cancel` 只能取消最新一次、先前分析的 chunk 推送无法停止。入口单飞拒绝 + `finally` 释放（防异常路径永久卡死）

**渲染层 / 业务层**
- M16 app.js `deleteNodes` 批量删除不停止后台监控、不清理监控配置（含凭据），被删设备的采集任务继续后台运行、继续写监控日志（单删路径均有）。与单删同口径 + `monitorCfgUndoList` 随快照带走供撤销恢复
- M17 app.js 路径分析：`ifText[i-1]` 按下标对应链路，但 bestPath 会把聚合组的多条成员链路展开为连续 lid（`linkIds.length > nodeIds-1`）——索引整体错位、后续跳段接口标注全部丢失。改逐跳查找 + 方向感知（源端接口在前）
- M18 app.js `onDbl` 无 text 分支：双击文本框落入 `editLink`（查不到 id 直接 return），而默认文案恰是「双击编辑文字」——引导用户走一条不通的路径。补 `editText` 分支
- M19 app.js 类型管理删除自定义类型只重写当前页节点：其他页节点 type 悬空，`_buildDefs` 只为现存类型生成渐变——切页后这些节点**卡片无底色**，悬空 key 随 saveGraph 持久化并进入导出/校验。改 `allSheetNodes()` 全页重写
- M20 app.js SNMP 识别回填软件版本：注释声明「不覆盖手填值」，条件 `n.osver !== info.version` 恰在两者不同时无条件覆盖。运行期 `snmpOsverSeen` 标记区分手填值与识别值，仅空值或未被修改的识别值可回填
- M21 render.js 框选节点不清 `selLinkIds`（与 `select('node')` 口径不一致）：框选后旧连线仍亮 `selected` 而 Delete 只删节点，高亮与行为不符；另 app 层 `select()` 无条件存点击入参、忽略 `renderer.select` 的返回值——Ctrl 点选取消后 `state.sel` 与画布实际选中集脱节（详情卡片显示、Delete 作用于已取消选中的元素）。两处对齐
- M22 model.js 表头判定：sa/sb 别名表含「设备1/设备2/A/B」等极常见设备命名，仅凭两端命中（hits≥2）把无表头数据的首行误判为表头**整行吞掉**（实测「设备1,设备2,…」两行导入丢 1 节点 1 链路）。判据提高为 ≥3 个角色命中（本项目导出与模板均为全列表头，非端点角色列作强佐证）

## 4. Low 级修复（31 项，摘要）

**监控/SNMP/诊断**
- v3 priv salt 由时间戳派生无唯一性保证（RFC 3414/3826 要求每消息唯一；同毫秒并发即 IV 重用泄露明文异或）→ 模块级单调计数器（31 位回绕）
- v3Request 的 Report 分支不校验 PDU request-id 即采纳 boots/time（能伪造源 IP 者可污染引擎缓存触发持续重发现循环）→ parseV3Message 补提取 Report 的 PDU rid（RFC 3412 6.3）+ 匹配校验
- `_pruneLogFiles` 可能删掉同设备其他管理口任务正持有写流的日志文件（Linux 删成功但句柄写已 unlink inode，磁盘不释放）→ 删除前跳过活跃 job 的 logPath
- cleanOid 允许每段最多 10 位数字（>2^32-1），berOid 按 32 位位运算编码**静默截断**成错误 OID（用户按厂商 MIB 文档填写则永远采不到数且无报错）→ 白名单拒绝段值 > 4294967295
- 独立备份指纹拒连路径直接 `fail()` 不摘除 status 监听器（共享 EventEmitter 上每次拒连泄漏一个持有 job/sid 闭包的监听器）→ 先摘除再 fail
- diag 主机白名单允许 `-` 开头：host 作为 ping/tracert 最后参数直传 spawn，`-n 1 -l65500` 等形态被解释为选项 → 拒绝

**主进程/更新器**
- 单实例锁失败后 `app.quit()` 是异步的，其后约 1700 行模块级初始化照常执行（与首实例并发访问同一 userData）→ 第二实例无待清理状态，`process.exit(0)` 兜底
- `certificate-error` 对非 webview 目标直接 `return` 不调 callback——该请求永久悬挂（Electron 契约必须回调）→ `callback(false)` 明确拒绝
- `monitor:logs-tree` 以 readdirSync 的**目录序** `slice(-300)` 后才过滤白名单——超 300 项的日期目录无差别丢文件（日志浏览器查不到实际存在的日志）→ 先按白名单过滤再取量（1000 兜底）
- `notifyUser` 的 Notification 对象未保活，GC 后告警通知可能不显示/点击失效 → 模块级 Set 保活（close 移除）
- updater `_download` 重定向分支对非 https 的 location 同步抛 `ERR_INVALID_PROTOCOL`（发生在响应回调内）→ uncaughtException 且该 Promise 永不落定 → 先校验协议再跟随

**util/model/layout/pdf/vsdx/render**
- `generateConfigs` 对悬空链路引用直接 TypeError（shortestPath/validateTopology 均有防御，此处遗漏）→ `linkOf.has` 防御
- `buildLinkFlow` 带宽回退用 `Number(l.bw)` 而非 `normalizeBw`：`bw:'1G'` 时 NaN → 利用率恒为 null → 统一走 normalizeBw
- `subnetOf` 不校验 bits 越界（`/64` 实际按 /32 移位但后缀原样输出非法 CIDR）→ 越界返回 null
- `sanitizeGraph` aMask/bMask 不钳制 0-32（vlans.mask 有钳制）——越界值导出回读被对端钳成 24，掩码静默漂移 → 同口径钳制
- sanitizeGraph 的 SAFE_ID 未排除 `__proto__/constructor/prototype`（能通过正则但命中原型 setter，干扰下游 byId 映射）→ BAD_ID 排除（sanitizeRegions 共用，提升模块级）
- `typeOf` 的 sw/srv/pc 裸子串无边界（rt 已修）：answer→switch、rpc→pc 误判 → 参照 rt 补边界
- `diffProjects` linkKey 以 `/` 连接接口名（天然含 `/`）再按 `/` split——展示错位且理论碰撞漏报 → 不可见控制符分隔
- `ipPlan` 接口行忽略链路掩码（/30 互联按默认 /24 归组，跨 /24 的 /23 互联被拆两段）→ 按链路掩码归组（buildSubnetTable 已正确使用）
- layout.js `tierLayout` 只列内置类型：自定义类型节点不进任何层、布局后原地不动与已移动节点错乱重叠 → 回退归入接入层（layerLayout 的 other 回退同口径）
- pdf.js `hasMgmt` 用 `n.mgmt` 单字段（画布/VSDX 均用 `nodeMgmts`）：mgmt 空而 mgmts 有值的合法数据在 PDF 中丢「管理：」行 → 统一口径
- pdf.js 带宽图例单行横排无折行：normalizeBw 通用分支可产出任意档位（155M/622M/2.5G…），超宽溢出画布 → 仿 VSDX 折行
- vsdx.js 标注文本框宽度按 96dpi 换算 pt 字号（低估 25% 且未计 TextBlock 默认边距）：长标注在 Visio 中溢出、防碰撞间距失真 → `/72 + 0.15`
- render.js wheel 缩放不处理 `deltaMode=1`（行单位）：Firefox 每格仅 ~0.5% 缩放近乎失效（浏览器直接打开 index.html 的支持路径）→ 归一像素
- render.js `bbox()` 不含文本框：fit()「适应视图」可把放在图外侧的说明文本框留在视野外（导出取景均已包含）→ 补 texts
- render.js 多选拖拽 move 热路径每帧每节点线性 find（O(N²)），几百台多选拖拽掉帧 → `byId` 映射
- app.js setHint 倒计时与模式生命周期脱钩：连线第二步提示被 ≤500ms 后的 tick 用旧文案覆盖；倒计时归零提示消失而模式仍激活（用户无感知连线/放置继续生效）→ 第二步走 setHint 重新发起；模式激活期间提示不再自动消失；setMode('normal') 清计时
- app.js 批量编辑的「管理Web页URL」未经 `normalizeWebUrl`（单设备编辑有）：无协议地址不会自动补 `http://`，危险协议不被拒绝 → 同口径

**服务/备份**
- svc-ftp：PWD 回复不对路径内双引号加倍转义（RFC 959，严格客户端解析失败）→ 转义；TYPE 参数大小写敏感（嵌入式设备常发 `type i` 收 504）→ 归一；认证失败计数表 fails 无上限（防伪造源撑表）→ 1000 封顶淘汰
- net-services `listFiles` 的 `.part` 过滤用 `includes('.part')`（比 FTP LIST 的 `endsWith||.part-` 宽）：`running-config.partial` 等正常文件在面板被隐藏 → 统一口径
- backup-store dir 为相对路径时 read/remove 的「相对拼接 vs 绝对前缀」恒不匹配被全拒（save 却能写）→ 构造函数统一 `path.resolve`
- shell-ui：SFTP 目录加载 busy 期间切标签的浏览请求被静默丢弃、在途结果又被 sid 校验作废——终态面板显示新会话、列表停留旧会话 → pending 记录补发；`sftpPaths` 随标签关闭不清理（缓慢累积）→ closeTab 清理

## 5. 核实后不修（附原因）

| 项 | 原因 |
| --- | --- |
| shell.js Telnet 用户写路径的 IAC(0xFF) 双写转义 | **误报**：写路径按 UTF-8 编码，合法 UTF-8 序列不可能产出 0xFF 字节（U+00FF 编码为 C3 BF），无注入面；NAWS 二进制载荷的 IAC 转义本就在位 |
| config-backup/backup-store 同秒并发 save 的 existsSync 竞态覆盖 | save 为纯同步函数，单线程事件循环内不可交错；跨进程双开路径已被单实例锁阻断 |
| monitor v3 空口令「静默降级」noAuth | normalizeV3User 的显式设计（与本轮 Trap 侧「按本端配置级别强制」口径一致：noAuth 是用户显式配置），改硬拒绝会破坏既有 noAuth v3 环境 |
| TFTP RRQ `readFileSync` 一次性同步读（≤32MB） | 有大小封顶；改流式需重构发送状态机，风险大于收益。列为待办 |
| svc-trap/monitor `berLen` 仅支持 0x82（65535） | 当前调用链受 Trap 包 64KB 上限保护不可达，属脆弱边界耦合。列为待办（与上项一起做流式重构时处理） |
| visio.js（VDX 遗留路径）平行链路重叠、不导出 texts/regions | 导出菜单已走 VSDX，buildVDX 仅测试引用——遗留格式冻结，恢复 VDX 导出时一并重构 |
| 孤立节点/设备备注无独立导出通道 | R6 §4 已记录的表格格式扩展待办（需扩列/扩行设计评审，影响 README 契约与旧版兼容） |

## 6. 验证

- 单元测试：1469 → **1509 全绿**（新增「回归：第七轮全面审查修复」40 项：CSV 表头判定 2、subnetOf 3、sanitizeGraph 2、typeOf 5、工程对比 2、配置生成 1、链路流量 1、三层布局 1、v3 salt/Report/noAuth 拒收 3、诊断白名单 1、指纹归属 2、RegexLab 并发 3、AI 拉取模型失败落定 1、OID 清洗 1、备份库相对路径 3、syslog 尾行 1、FTP TYPE/PWD/STOR 断开/fails 封顶 4、并发应用配置 2、文件编目 1）
- e2e（无头 Chrome）：34 段全绿两遍、无控制台错误（含多图纸、删除级联撤销、聚合组、导出链）
- 服务端修复均以真实 socket 端到端验证（FTP 迷你客户端实测 stop 中断后无成品文件/无收件广播/无 .part 残留；syslog 实测无尾换行尾行入库）
