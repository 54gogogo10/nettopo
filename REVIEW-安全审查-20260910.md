# NetTopo 安全审查报告 — 2026-09-10（第二轮）

> **修复记录（2026-09-10）**：本报告发现的问题已按推荐优先级全部修复——High 1/1、Medium 5/5、Medium-Low 2/2、Low 18/22（L7/L10 维持架构取舍，L4 部分缓解，L19 说明性）。新增 40 项回归单测（1530 → 1570 全绿）。各项修复见 §9。

> 本轮为 2026-09-02 首轮安全审查（9M/15L，修复 26 项）之后的全面复审，重点覆盖第八~十轮功能修复（约 77 项改动）引入的新面与首轮盲区（Trap 接收器 / snmp-v3 轮询客户端路径）。

## 1. 范围与方法

- **方式**：6 路并行专项深审（① Electron 主进程/IPC/升级链 ② 主窗口渲染层注入面 ③ Shell/Webview 独立窗口 ④ 主进程 Node 模块路径/命令注入 ⑤ 内置网络服务+SNMP 协议解析器 ⑥ 导入导出解析器/AI 模块），全量通读约 2.9 万行源码；全部 High/Medium 发现经人工回读源码逐行复核确认。
- **威胁模型**（沿用首轮并扩展）：① 局域网任意主机向 TFTP/FTP/Syslog/**Trap 162** 发包（本轮新增重点）；② 恶意/被入侵设备端（SSH/Telnet/SNMP v2c/v3 输出均不可信）；③ 恶意工程/CSV/Excel 文件；④ 被注入的渲染层（IPC 面=纵深边界）；⑤ 用户自配的 LLM 端点。

## 2. 总体结论

Electron 基线与首轮修复（路径三层防线、escHtml 出口纪律、凭据打码、缓冲上限、TFTP/FTP/Syslog 协议面）在三轮大改中**均保持无回归**；本轮确认 **High 1 项、Medium 5 项（其中 2 项 Medium 为首轮修复的条件性残余）、Medium-Low 2 项、Low 22 项**。新增高危集中在** Trap 接收器（首轮盲区）**与 **SNMP v3 轮询客户端路径（第八轮只修了接收侧）**。

## 3. High 级发现

### H1 Trap OID/varbind 解码无长度上限：无认证单包触发内存放大 + 磁盘灌满（js/svc-trap.js:53-62, 219, 329, 389, 105；js/snmp-v3.js:70-79, 84-94）

`decodeOid` 对任意长度 body 解码：分量按 `(v<<7)|byte` 累积（32 位移位使分量十进制上限约 10 位），64KB body → 约 1.3 万分量 → **~130KB 十进制串**。防线缺口链：

- `parseVarbinds`（svc-trap.js:219）`oid: decodeOid(...)`、OID 类型值（tag 0x06）同走 `decodeOid`，均无长度钳制；`cleanText` 的 `MAX_TEXT=300` 只约束字符串值，**不约束 OID 键与 OID 值**；
- `summary`（:329/:389）`v.oid+'='+v.value` 拼接、`trapNameOf`（:105）`'enterprise（'+s+'）'`、`ent.oid` 三处均无截断 → 单条事件 msg/trap/oid 合计可达 ~260KB（包长 64KB 的 4 倍文本放大）；
- 落点：环形缓冲 1000 条（**~260MB 稳态驻留**）+ `_writeEntry` 每条整行归档（限速 100pps × ~200KB ≈ **20MB/s 持续写盘**，写的是当日文件、`keepDays` 清理不触达）+ `tail()` 推渲染层巨串 + `recordMonitorEvent`（electron-main.js:142 的 `t.trap` 无 slice，对比 `t.msg` 有 120 截断）；
- **v2c 无任何认证即可触发**（v1/v2c community 见 M2）；v3 路径同病（snmp-v3.js `parseVbs→decodeValue`），且 v3 的 OCTET STRING 值（:87）连 MAX_TEXT 都没有、换行也不折叠（见 ML1）。

**修复方向**：`decodeOid` 输出限长（如 256 字符，OID 现实上限 ~128 子标识符）；varbind 值统一走 `cleanText` 口径（控制字符+换行折叠+300 截断）；`summary`/`trapNameOf`/`ent.oid` 出口再截断兜底；`recordMonitorEvent` 对 trap 补 slice。

## 4. Medium 级发现

### M1 SNMP v3 轮询客户端不强制 authenticated/decrypted：明文抢答伪造监控数据（js/monitor.js:323-345；js/snmp-v3.js:353-355, 383-397）

`parseV3Message` 的 `wantAuth` 取自**包内自报 flags**（snmp-v3.js:354），本端配置了认证用户时若收到 flags=0 的明文包即跳过验签直接解析；`v3Request` 成功后只核对 engineID/pduTag/rid（monitor.js:329-345），**不复查 `pr.authenticated/pr.decrypted`**。对比：Trap 接收侧第八轮已补同口径强制（svc-trap.js:375-378「本端用户要求认证时未认证包一律丢弃」），轮询客户端侧漏网。

攻击：用户以 **authNoPriv** 轮询设备时，rid/userName/engineID 全部明文可嗅探——同网段攻击者抢答回 flags=0 明文 0xa2 响应（rid 抄请求值、来源 IP 通过 :289 校验）即可注入伪造 sysUpTime（假「设备重启」告警）、CPU/内存/接口流量、ifOperStatus（掩盖接口 Down）。authPriv 时 PDU 加密、rid 不可见（需猜 31bit），不可行。

**修复方向**：与 Trap 侧对齐——`user.level !== 'noAuth' && !pr.authenticated` 拒收、`authPriv` 时 `!pr.decrypted` 拒收。

### M2 v1/v2c Trap 无 community 校验：零门槛告警注入与设备冒充（js/svc-trap.js:130, 330-347；js/net-services.js 构造未传）

`parseTrapPacket` 提取 community 后仅作展示/归档字段；`TrapServer` 无 community 配置项（net-services.js 亦未传）。任意 LAN 主机发 v2c linkDown（community 随便填）→ 按 `t.standard` 弹系统通知、`monitor.status()` 按源 IP 匹配到真实设备则记入其事件时间线并归档——无源认证的告警注入/设备冒充。对照：TFTP/FTP/Syslog 均有口令或规则，Trap v1/v2c 是唯一零门槛入口（v3 有 USM 强制）。

**修复方向**：面板 Trap 节加可选 community 白名单（空=收全部，兼容现有部署；配置后 v1/v2c 比对不符丢弃计数）。

### M3 AI API Key 外泄链：http:// baseUrl + 已存 Key 明文出网（electron-main.js:1437-1441；js/ai-llm.js validateBaseUrl 放行 http:）

`ai:set-config` 接受任意 `http://` baseUrl 且**不触碰 Key 字段时已存 Key 原样保留**；`ai:test`/`ai:analyze`/`ai:list-models` 随后即带 `Authorization: Bearer <Key>` 明文请求该地址。被注入的渲染层两次 invoke（无需读到 Key）即可把用户已保存的 API Key 送往攻击者服务器。「Key 明文不出主进程、回显脱敏」（:1430）这道专门防线被从 baseUrl 出口绕过；渲染层 CSP 禁直连外网，主进程代发不受 CSP 约束。http 是为本地 Ollama 场景有意放行的，但「已存 Key + http baseUrl」组合使 Key 明文出网。

**修复方向**：已存 apiKey 非空时 baseUrl 强制 https（用 http 需先清空 Key）；或 http baseUrl 请求不自动附带已存 Key。

### M4 will-navigate 放行任意 file: 目标：preload 全桥随导航泄露（electron-main.js:1787-1790）

```js
if (!/^file:/i.test(String(url))) ev.preventDefault();  // 只查协议、不查目标文件
```

宿主窗口 file→file 导航不受 Chromium 拦截，preload 对该 webContents 恒定——导航到任意本地 HTML 后新页面照常获得 topoShell/topoSecure/topoUpdate 全套 IPC 桥。配合内置 TFTP/FTP 收件目录（`userData/net-services/{tftp,ftp}/<ip>/`，路径可预测、**文件内容完全由远程发送方控制**）：开启 FTP/TFTP 时 LAN 攻击者可预先投递无 CSP 的 evil.html，再经被注入脚本触发一次 `location='file://...evil.html'` 即完成 **CSP 逃逸**（把 CSP 挡住的半成品 XSS 升级为可加载任意脚本/外联的完整沦陷页面 + 全 IPC 桥）。

**修复方向**：file: 也做白名单——仅放行 `__dirname` 下 index/shell/webview 三页（绝对路径比对），其余 file: 一律 preventDefault。

### M5 FTP 默认口令残余：仅面板保存路径拦截，服务端不强制（js/net-services.js:26, 39-45；UI 拦截 app.js:8271-8275）

首轮 M9 修复为「面板保存并应用时自动换随机口令」（在位）；但服务端 `defaultConfig` 仍 `nettopo/nettopo`，`cleanCred` 空值回退默认（用户无法配置空口令）。残余触发面：旧版已启用 FTP 且未改口令的用户**升级后从不打开面板保存**——默认口令持续生效，LAN 任何人可 STOR 植入/RETR 读取/DELE 删除。防爆破封禁对持有正确默认口令者无效。

**修复方向**：主进程 applyConfig 时检测「enabled 且口令=默认值」→ 自动生成随机口令并系统通知（与面板口径对齐、不依赖 UI 路径）；或启用时强制要求设置口令。

## 5. Medium-Low 级发现

### ML1 v3 varbind 值不折行不截断：归档行注入（js/snmp-v3.js:87）
`case 0x04` 字符类刻意排除 0x09/0x0a/0x0d，`\r\n` 原样保留且无 MAX_TEXT——v3 Trap 字符串值内嵌换行可伪造归档审计行（与首轮修掉的 syslog 换行折叠同类；v1/v2c 路径 `cleanText` 已折行，v3 走 snmp-v3 的 `parseVbs/decodeValue` 漏网）。持有 v3 凭据的设备端或本端配了 noAuth 用户时可达。**修复**：v3 值统一走 cleanText 口径（与 H1 一并修）。

### ML2 Trap 主机目录无上限：伪造源 IP UDP 无限建目录（js/svc-trap.js:445-461, 473-493）
无 syslog 的 `MAX_HOST_DIRS=1024` 等价物，`_cleanupOld` 只删过期 `.log` 不删空目录。可发伪造源 IP UDP 的 LAN 环境下每伪 IP 一个目录持续累积耗尽目录项。**修复**：对齐 syslog 的目录封顶 + 空目录回收。

## 6. Low 级发现

| # | 位置 | 问题 |
| --- | --- | --- |
| L1 | monitor.js:752, 1904 | ICMP 探测 host 仅剔控制字符、不拒首 `-`（diag.js 的 isValidDiagHost 有此防线）——`-t` 等作 ping 选项（spawn 数组无 shell 注入，仅探测行为异常）；建议复用 isValidDiagHost |
| L2 | monitor.js:1520 | SNMP ifDescr（设备可控）不剥控制字符：`\n`/ESC 注入事件时间线 detail 与 AI 巡检日报行（shell/syslog/trap 三路径均已剥，SNMP 路径漏网） |
| L3 | shell-ui.js:1549-1558 | 命令历史（群发/AI/快捷按钮 ≤512 字 ×60 条）明文持久化 shell 窗 localStorage——`password xxx` 类命令明文驻磁盘、无清理入口，与书签密码 DPAPI 口径不一致 |
| L4 | ai-llm.js:160-210; shell-ui.js:1237-1240 | AI 助手 auto 模式：设备可控终端上下文（MOTD）注入提示词可直接驱动命令下发（默认 confirm 有缓解）；建议 auto 模式对删除/擦除类关键词仍强制确认 |
| L5 | electron-main.js:695-704, 1768 | web:cert-allow 的 id 为可枚举自增（cert1..N）：webWin 内脚本可循环放行任意挂起证书（含 MITM 坏证书），确认弹窗形同虚设；id 应加随机熵并绑定「已推送展示」 |
| L6 | electron-main.js:759-766 | shell:data/close/resize 只校验 sender 窗口、不校验会话 owner：Shell 窗渲染层可向 monitor 后台会话注入命令（绕过监控 readOnly）或掐断连接 |
| L7 | electron-main.js:927-940, 1293-1296 | secure:decrypt 为无来源约束解密 oracle、netsvc:get 回传 FTP 明文口令——XSS 渲染层可解密工程内全部设备密码。DPAPI 同用户本可解密属架构权衡，建议文档化 + netsvc:get 口令改掩码（编辑时才取） |
| L8 | electron-main.js:516-523 | shell:record-append 主窗口亦可调用：可向录像追加伪造 JSONL 行（≤1MB/次、次数不限），审计录像完整性无保障；建议收紧到 shellWin 且绑定会话 |
| L9 | electron-main.js:1705-1707 | defaultSession（主窗/Shell 窗）未装权限 handler，与 webPartition 的 deny-all 不一致；建议补同口径（仅 fullscreen） |
| L10 | updater.js:276-309 | exe 与 .sha256 同源：仓库/源端被接管时配套哈希照样通过（SHA256 只防传输损坏）；:290 release size=0 时跳过大小比对（有 SHA256+400MB 兜底）。建议升级签名校验或独立渠道公示哈希 |
| L11 | app.js:4924-4928 | openReleasePage 把 GitHub API 的 html_url 直传 openExternal，无协议/host 白名单（TLS 保护下当前不可利用）；建议限定 `^https://github\.com/` |
| L12 | layout.js:219, 251; util.js:1406-1409, 2743-2744; app.js:1825 | 第九轮 maxOf 修复不完整：layer/tier 布局 cellW、subnetGroups、alignNodes、IP 规划列宽仍 `Math.max(...)` spread——大节点数工程触发 RangeError 崩布局功能 |
| L13 | app.js:2822 | loadProject 打开 .nettopo 工程无文件大小上限（CSV/Excel 导入有 20MB）：超大文件同步 JSON.parse 冻结渲染线程 |
| L14 | ai-llm.js:680, 703 | `Object.assign({}, usage, r.usage)` 合并服务端 SSE usage：`__proto__` 自有键触发原型 setter（最终均经 Number() 入 textContent，无代码执行）；建议 Object.hasOwn 过滤 |
| L15 | snmp-v3.js:395; svc-ftp.js:227 | HMAC/口令比较非常量时间（equals/===早退）；建议 crypto.timingSafeEqual |
| L16 | svc-trap.js:350-351 | v3 Trap 无 USM 时间窗（注释自认）：捕获合法已认证 Trap 可无限重放刷告警（限速缓解），记录在案 |
| L17 | snmp-v3.js:101-114 | passwordToKey 逐包 1MB 哈希、engineID 为包内值无法缓存：默认 1 用户 ≈100pps×2MB≈200MB/s 哈希（~20-40% 单核），8 用户×8 倍；建议按 user+engineID 缓存 Kul |
| L18 | svc-syslog.js:63-64 | RFC5424 tag（`(\S+)` 可含 ESC/BEL）未剔控制字符，落盘行与终端复制面污染；msg/host 已清洗，tag 漏网 |
| L19 | svc-syslog.js:399, 459-460 | 设备报未来时间戳（9999 年）产生永不过期的日志文件（hostDirs 1024 封顶缓解规模） |
| L20 | app.js:8317; app.js:3823; app.js:7864 | 渲染层三处防御深度缺口：nsv-badge `f.svc` 裸拼 class（当前主进程硬编码常量）、openModal option `value` 未转义（当前调用点全白名单值）、fmtDiff 失败分支 error 拼接（当前死代码）——均一行级 escHtml 兜底 |
| L21 | shell-ui.js:1284-1286 | showCtx 右键菜单 label 直接 innerHTML（当前全静态文案）：未来把设备数据拼进 label 即成注入 sink；建议改 DOM API |
| L22 | electron-main.js:536, 548 | record-list/read 用 statSync（跟随符号链接），偏离项目 lstat 惯例（白名单+前缀终判齐备，仅纵深一致性） |

**Info（记录不修）**：webview 设备页共享 `persist:nettopo-web` 分区（跨设备 Cookie 驻留，功能取舍）；粘贴分片无总量上限（本机/设备 DoS 面）；FTP LIST 本地手工放入的文件名不转义 CR（威胁模型外）；STOR/TFTP 写流无背压（有界）；`_writeEntry` lstat→write TOCTOU（本机同用户前提）；TFTP RRQ 重复 ACK0 双触发取块（块号单调，自扰）。

## 7. 已检查且确认安全（要点）

- **Electron 基线无回归**：三窗口 contextIsolation+sandbox、setWindowOpenHandler deny、webview guest popup 仅 http(s)、证书 host+指纹记忆+pendingCert FIFO 32、双分区下载强制另存、webPartition 权限仅 fullscreen、will-attach-webview 剥 preload、update:download 忽略渲染层资产（第十轮修复在位，仅 https+SHA256+400MB）、apply 仅主进程 pendingFile+psQuote、reveal 前缀终判、testClose 环境门控、uncaught 兜底。
- **IPC 文件名通道全量复核**：record/backup/backupcfg/ai-history/monitor-logs/netsvc-file/syslog-read 八族均「正则白名单+前缀终判+限长+上限」，无渲染层可直达任意路径的通道；diag 五入口 host 白名单+拒 `-` 开头+spawn 数组参数（零 exec）。
- **渲染层 XSS**：09-03 后三轮新增 UI（监控中心/叠加层/日报/AI/网段扫描/表格导出）166 处 innerHTML 逐点回溯——出口纪律保持（escHtml/textContent/createElementNS），AI 全链路 textContent 无 HTML 出口，CSP 三页无放宽，无 eval/newFunction/postMessage/URL 参数/事件属性拼接回归；表格导出 sanitizeCell 覆盖 CSV 公式注入全部 3 处出口。
- **上轮协议修复无回归**：syslog 换行折叠/maxConnections/尾部检索、FTP PASV 对端校验/PORT 同源十进制/writableLength/速率/防爆破、TFTP 会话配额/RRQ 流式化 fd 清理、SNMP v2c rid 盐+来源校验、BER 边界（0x80-0x84、≤8 字节整数、无递归）、USM 验签先于解密/priv 密钥派生/privSalt 单调。
- **凭据**：Key/口令 enc1: 密文落盘、明文不出主进程（M3 的 baseUrl 绕过除外）、三重打码在位、Telnet IAC 边界、SFTP 本地路径全部对话框产生、跳板/目标独立指纹队列。
- **导入导出**：XLSX 0.20.3（已知 CVE 均修）只取 cell.v/日期 w 不取公式；VSDX SVG-as-image 无脚本执行面；VDX 新增拼接全经 escXml；regex-lab worker 协议校验+超时处决+拉黑在位。

## 8. 修复优先级建议

1. **立即**：H1（Trap OID/varbind 长度钳制——无认证单包、内存+磁盘双放大，兼修 ML1/L2 的 cleanText 统一口径）→ M1（v3 轮询 authenticated/decrypted 强制，与 Trap 侧一行对齐）。
2. **短期**：M2（Trap community 白名单）、M4（file: 导航白名单三页）、M3（http baseUrl 不带已存 Key）。
3. **随迭代**：M5（applyConfig 服务端强制改默认口令）、ML2、L1–L22（多为一行级）。

## 9. 修复记录（2026-09-10）

**验证**：单测 1530 → 1570 全绿（新增 40 项「安全修复回归（第二轮）」）；全部改动文件 `node --check` 通过；导航白名单经真实路径实测（允许三页 / 拒绝同目录与父目录其它页面 / 拒绝远程）。

### High（1/1 全修）

| # | 修复方式 |
| --- | --- |
| H1 | 两处 `decodeOid`（svc-trap.js:53、snmp-v3.js:70）加子标识符数量上限（128）与文本长度上限（256）；新增 `summarize()` 对 varbind 汇总限量 8 条 + 总长 800 截断（`_ingest`/`_ingestV3` 两处替换内联拼接）；v3 侧新增 `cleanText`（控制字符剔除 + 换行折叠 + 300 截断），`decodeValue` 的 OCTET STRING 改走它 → 单条 Trap 事件的内存/磁盘放大从 ~260KB 降到有界。 |

### Medium（5/5 全修）

| # | 修复方式 |
| --- | --- |
| M1 | monitor.js `v3Request` 在 pduTag/rid 校验后新增：`user.level !== 'noAuth' && !pr.authenticated` 拒收、`user.level === 'authPriv' && !pr.decrypted` 拒收——与 Trap 接收侧（svc-trap.js:375-378）同口径，明文抢答不再能伪造监控值。 |
| M2 | TrapServer 新增 `communities` 白名单（services 归一化 + 新配置项 `trap.community`，逗号分隔 ≤16 个）；`_ingest` 在 v1/v2c 解析后比对，不符即丢弃并计入 `communityReject`；面板新增「团体字」输入 + 状态栏显示「团体字白名单（已拒 N）」+ 命令示例随配置渲染；默认空 = 收全部（兼容既有部署）。 |
| M3 | `aiCfgFromSettings` 新增：已存 Key 非空且 baseUrl 非 https 时**不回传该 Key**（http 端点不自动携带已存 Key）；`apiKeyStored` 用于「已配置」提示；`ai:list-models` 的表单回退同口径（仅 https 回退）；`ai:get-config/set-config` 增 `apiKeyHttpBlocked` 标记，面板 Key 占位符提示「已保存但当前为 http 地址，不会自动发送」。 |
| M4 | `will-navigate` 由「仅查 file: 前缀」改为白名单判定 `isAllowedLocalPage`：URL 归一 → 仅 file: → pathname 去 Windows 前导斜杠 → `path.resolve` 后比对 `__dirname` 与 `app.getAppPath()` 下 index/shell/webview 三页（大小写归一），其余一律 `preventDefault`；堵死 TFTP/FTP 收件目录投递本地页 → 导航 → preload 全桥泄露的链路。 |
| M5 | `normalizeConfig` 新增：FTP 启用且口令仍为默认 `nettopo` → 服务端自动生成 16 位随机口令并置 `_ftpPasswordChanged`；`netsvc:set` 改以**生效配置**落盘（避免每次重启重新生成、设备侧 copy 口令失效）并从落盘对象删除内部标记；启动恢复路径（`applyConfig(restored)`）同样把替换后的密文回写 settings；面板消费 `ftpPasswordChanged` 回填表单并提示。 |

### Medium-Low（2/2 全修）

- **ML1 ✅** snmp-v3 `decodeValue` 的 OCTET STRING 改走新增 `cleanText`（换行折叠 + 300 截断），v3 Trap 字符串值无法再伪造归档行。
- **ML2 ✅** TrapServer 补 `hostDirs` 缓存 + `MAX_HOST_DIRS=1024` 封顶（超限来源只进环形缓冲不落盘）+ `_cleanupOld` 清空来源目录（`rmdir` 仅在空目录成功；有活跃写流的来源跳过）——与 syslog 同口径，堵死伪源 UDP 无限建目录。

### Low（18/22 修复）

- **L1 ✅** monitor `_validate` 的 host 补拒 `-` 开头（ICMP 探测选项注入）。
- **L2 ✅** SNMP `ifDescr` 回填剥控制字符（不再污染事件时间线/AI 日报）。
- **L3 ✅** shell 窗命令历史：新增 `SENSITIVE_CMD_RE`，`password/secret/community/passphrase/token/api-key` 等凭据类命令不入 localStorage（读取时也过滤存量）。
- **L4 ✅（部分）** AI auto 模式新增 `DESTRUCTIVE_CMD_RE`：命中 erase/format/delete/reset/reload/reboot/shutdown/factory-reset/`no interface|vlan|ip route|router` 等破坏性动词时，即使 auto 也降级为人工确认（提示注入产物不再无条件执行）。
- **L5 ✅** 证书确认 id 改 `crypto.randomBytes(16).toString('hex')`（不再可枚举）；移除 `certSeq`。
- **L6 ✅** shell.js 新增 `ownerOf(id)`；`shell:data/resize/close` 增加 `shellUiSession(id)` 归属校验，只允许 Web Shell 窗口操作 `owner==='ui'` 的会话（后台监控会话不可被注入命令/掐断）。
- **L8 ✅** `shell:record-append/stop` 绑定 `shellRecOwner`（发起录制的 webContents），其它窗口不可追加伪造录像行；`record-start` 重置归属。
- **L9 ✅** defaultSession（主窗/Shell 窗）补装与 webPartition 同口径的 `setPermissionRequestHandler/CheckHandler`（仅放行 fullscreen）。
- **L11 ✅** `openReleasePage` 限定 `^https://github\.com/<owner>/<repo>` 才采用响应 URL，否则回落固定发布页。
- **L12 ✅** util.js 新增 `U.spreadMinMax`；`subnetGroups`、`alignNodes`、IP 规划列宽、layout 的 layer/tier `cellW` 全部收敛（`grep Math.max(...)` 已归零，仅余注释）。
- **L13 ✅** `loadProject` 补 20MB 上限（file.size + buffer.byteLength 双重判定）。
- **L14 ✅** ai-llm.js 新增 `mergeUsage`（白名单拷贝 5 个用量键），替换 5 处 `Object.assign({}, …, j.usage)` 合并——服务端 `__proto__` 自有键不再触发原型 setter。
- **L15 ✅** snmp-v3 认证摘要比较改 `crypto.timingSafeEqual`；svc-ftp 口令/用户名比较改新增 `timingSafeStrEq`。
- **L17 ✅** snmp-v3 `passwordToKey` 加有界 FIFO 缓存（`_kulCache`，键 = 算法+engineID+口令，上限 64）——Kul 只由三者决定，Trap 每包 1MB 哈希开销消除。
- **L18 ✅** syslog `parseSyslogMsg` 出口对 `tag` 剔控制字符 + 截 48（与 msg/host 同口径）。
- **L19 ✅** syslog `_writeEntry` 落盘日期不晚于本机当日（未来时间戳折到当日归档），不再生成 9999 年等永不过期文件（消息 ts 原样保留供展示）。
- **L20 ✅** app.js 三处防御深度：`nsv-badge` 的 `f.svc`、openModal 两处 `<option value>`、`fmtDiff` 失败分支 error 均补 `U.escHtml`。
- **L21 ✅** shell-ui `showCtx` 右键菜单 label 改 `escAttr`（shell 窗内实际可用的转义函数）。
- **L22 ✅** `shell:record-list` 与 `record-read` 由 `statSync` 改 `lstatSync`（对齐项目拒符号链接惯例）。

### 未修 / 维持（4 项，均为架构取舍或需单独迭代）

- **L7（维持）**：`netsvc:get` 回传 FTP 明文口令、`secure:decrypt` 为无来源约束解密 oracle——DPAPI 绑定用户级、同用户任意进程本可解密，属架构权衡；且面板需展示 FTP 口令供设备侧 `copy` 命令（掩码会破坏功能）。已在报告与 README 安全边界说明中记录。
- **L10（维持）**：升级包 exe 与 .sha256 同源，SHA256 仅防传输损坏、不防源端投毒；彻底解决需代码签名（Authenticode）/独立渠道公示哈希，属发布链单独迭代。
- **L16（维持）**：v3 Trap 无 USM 时间窗（重放有 100pps 限速与通知节奏缓解）——设计取舍已在代码注释记录。
- **L4 剩余**：破坏性命令启发式无法穷举所有设备语法；确认模式的提示注入风险由「默认 confirm + 系统提示禁令 + 数据分隔包裹」多层缓解，auto 降级为补充防线。

### 新增单测（40 项，「安全修复回归（第二轮）」节）

H1 超长 OID×4、ML1 折行/限长×3、M1 明文包属性/判定式×3、M2 白名单/归一化×5、ML2 启动×1、M4 导航白名单×5、M3 https 判定×3、M5 口令替换×4、L1×1、L2×1、L18×1、L19×1、L15×1、L17 缓存×2、L12 极值×2、L14 原型×2。

**验证**：1530 → 1570 全绿；全部改动文件 `node --check` 通过；导航白名单经真实路径实测（允许三页 / 拒绝同目录与父目录其它页面 / 拒绝远程）。

## 10. 与首轮报告的衔接

- 首轮 L2/L5/L14（TOFU 首连无人工确认、Web Shell 指纹确认在渲染层、FTP STOR overwrite TOCTOU）维持原结论未重开。
- **更正**：首轮 A4（electron-fuses）经核对**实际已完成**（`build/afterPack.cjs` + commit `c2e50ae`/`b95000d`：关闭 RunAsNode/NODE_OPTIONS/--inspect、启用 asar 完整性校验与仅从 asar 加载，win/linux 共用钩子）——首轮报告 §8「A4 未做」及本报告早前版本对本项的转述有误，以此为准。
- 首轮 M9 本轮复核：UI 拦截在位，残余条件路径升级为本轮 M5（已修）。

## 11. 尚未修改（本轮修复后仍存的已知项）

以下为**明确未修**的项，按性质分类；均为低危/信息级或架构取舍，不构成立即风险。

### 11.1 本轮报告已注明「维持」的定级项（4）

| # | 项 | 未修理由 |
| --- | --- | --- |
| L7 | `netsvc:get` 回传 FTP 明文口令、`secure:decrypt` 为无来源约束解密 oracle | DPAPI 绑定系统账户、同用户任意进程本可解密，属架构权衡；且面板需展示 FTP 口令供设备侧 `copy` 命令，掩码会破坏功能 |
| L10 | 升级包 exe 与 .sha256 同源，SHA256 仅防传输损坏、不防源端投毒 | 彻底解决需代码签名（Authenticode）/独立渠道公示哈希，属发布链单独迭代 |
| L16 | v3 Trap 无 USM 时间窗（可重放已认证 Trap） | 设计取舍，已有 100pps 限速与通知节奏缓解 |
| L4 | AI auto 模式破坏性命令判定为启发式，无法穷尽设备语法 | 已加降级确认作补充防线；确认模式本身的多层缓解为兜底 |

### 11.2 本轮 Info 级（报告「记录不修」，6）

- webview 设备页共享 `persist:nettopo-web` 分区（跨设备 Cookie 驻留，功能取舍）
- Shell 粘贴分片无总量上限（本机/设备 DoS 面）
- FTP LIST/NLST 对本地手工放入 rootDir 的文件名不转义 CR/LF（威胁模型外，服务自身创建的路径已清洗）
- FTP STOR / TFTP WRQ 写流未按背压 `drain`（内存上限有界）
- `_writeEntry` 的 lstat→writeStream TOCTOU（本机同用户前提）
- TFTP RRQ 重复 ACK0 可并发触发一次取块（块号单调，仅客户端自扰）

### 11.3 本轮审查提出、但未列入报告编号的次要项（未修）

- `js/shell.js:135/365`：`connect()`/oneshot 的 `password` 无长度上限（monitor 侧钳 1024，shell 直连 IPC 未钳）——仅内存驻留差异
- `js/svc-ftp.js:284`：`pasvPending.timer` 未 `unref`（会话销毁后 30s 定时器仍挂事件循环，仅延迟进程退出）
- `js/monitor.js:326-341`：引擎发现 Report 可被嗅探 msgID + 竞态投毒（DoS 有限）
- `js/svc-trap.js`：v3 已认证 Trap 字符串值上限 300 对多 varbind 汇总的约束（H1 已间接收敛，未单列）
- `shell:open-external` / `web:open` 未区分 host、剪贴板读写通道开放（功能所需，仅供威胁建模参考）
- `shell:reconnect` 未做会话归属校验（monitor 会话结束时参数即删，实际不可重连；仅一致性缺口）

### 11.4 首轮（2026-09-02）延续未修（4）

- 首轮 **L2**：monitor 首连自动信任主机指纹，无用户人工确认（信任库「查看/撤销」管理入口已于后续 commit `c2e50ae` 补齐，**仅剩首连确认**）
- 首轮 **L5**：Web Shell 的 TOFU 指纹确认与记忆在渲染层（架构性纵深边界；monitor 侧主进程信任库不受影响）
- 首轮 **L14**：FTP STOR `overwrite=false` 的 TOCTOU（已加 rename 前复核，窗口缩至毫秒级）
- 首轮 **M2 剩余**：`shell.on('output') → webContents.send` 的渲染层背压（渲染层冻结时队列在 Electron 内部，无可行测点）


