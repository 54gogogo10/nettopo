# NetTopo 复审报告（R2）— 2026-08-14

## 1. 执行摘要

- **结论:** 风险等级 **B(良好)**。未发现可远程利用的 Critical/High 级**应用**漏洞;唯一 High 项位于构建期依赖链,已移除。
- **审查范围:** electron-main.js、preload.js、js/ 全部 12 个模块、3 个 HTML 入口、备份库、构建脚本与依赖树;4 个并行子代理分审 app.js / util.js / render+model+layout / pdf+visio+vsdx,关键疑点由主审逐一复核。
- **修复状态:** 本轮确认的短期/中期项 **已全部修复并验证**(用户确认「修改」);回归 385/385、npm audit 清零、smoke-shell 55 项全绿。
- **发现统计:** High 2(构建期)/ Medium 0 / Low 8 / 正确性 中 2 低 5 / Info 若干。

---

## 2. 安全发现(按严重度)

### F-1【High·仅构建期】extract-zip 符号链接路径穿越(CVE-2026-56876)— ✅ 已修复
- **位置:** package.json:19 electron-packager ^17.1.2 → extract-zip
- **证据:** `npm audit --registry=https://registry.npmjs.org` 报 2 High;[GHSA-jmr9-qjv8-65gv](https://deps.dev/advisory/osv/GHSA-jmr9-qjv8-65gv) / [CVE-2026-56876](https://app.opencve.io/cve/CVE-2026-56876):解压含符号链接的 zip 未校验规范路径。**全版本受影响,无修复版**。
- **影响:** 仅构建机;打包产物不含该库,终端用户零暴露。
- **修复:** 移除 electron-packager devDep 与 `pack` 脚本(dist 由 electron-builder 构建,`pack` 无任何引用)→ `npm audit` **0 vulnerabilities** ✓

### F-2【Low】index.html CSP 'unsafe-eval' 无实际必要 — ✅ 已修复
- **位置:** index.html:7;README.md:122 的保留理由经实测不成立(xlsx.full.min.js 709KB 中 eval( 出现 0 次、无 new Function;应用代码亦无)。
- **修复:** 移除 'unsafe-eval';README 声明同步修正;新增 Electron 冒烟验证(test/_csp-check.js,gitignored):**零 CSP 违规,XLSX/TopoUtil/TopoRender/状态栏全部正常** ✓

### F-3【Low】`web:open` IPC 处理器缺 sender 校验 — ✅ 已修复
- **位置:** electron-main.js:207
- **修复:** 仅允许 `e.sender === mainWin.webContents`,其余窗口返回 forbidden ✓

### F-4【Low】宿主窗口无导航守卫 — ✅ 已修复
- **风险:** 主窗/Shell 窗加载远程页面将把 preload 桥(topoShell=任意 SSH/Telnet、topoBackup)一并泄露。当前无触发路径(全部本地内容、textContent 渲染),属纵深防御。
- **修复:** `web-contents-created` 中:宿主窗口 `will-navigate` 仅放行 file://、`setWindowOpenHandler` 一律 deny;**webview guest 豁免**(设备页内嵌浏览器自由导航,popup 仍由原有双层拦截转标签)✓ smoke-shell 55 项验证无回归。

### F-5【Low】CSV 公式注入防护前导空白绕过(CWE-1236)— ✅ 已修复
- **位置:** js/util.js:138 sanitizeCell;" =1+1" 可绕过前缀(Excel 先 trim 再判公式)。
- **修复:** 先剔除 `^[\s\uFEFF]+` 再判定 `^[=+\-@]`,原值整体加 `'` 前缀;回归测试: `' =1+1'`/`'\t@x'` 加前缀、`' ok'` 不受影响 ✓

### F-6【Low】XML 导出未剔除非法控制字符 / cell() 零转义 — ✅ 已修复
- **位置:** js/util.js:48 escXml;js/vsdx.js:144-146 XRAW/cell
- **修复:** escXml 追加剔除 `[\x00-\x08\x0B\x0C\x0E-\x1F]`(防复制粘贴带入不可见字符致 Visio 拒开);XRAW 统一改用 escXml;cell 的 V 值经新增 ATTR() 转义(`& " < > '`),extra 注释固化为硬编码字面量;回归:escXml 控制字符剔除 ✓

### F-7【Low】其余纵深防御项(未修改,已论证不可利用)
- app.js:1953 `<option value="${v}">` 未转义——全部 4 个调用点 value 来自 SAFE_ID 白名单/硬编码;app.js:731 网段 value 经 subnetOf 严格 IP 正则约束。
- app.js:172/853/1217 导入无大小上限(本地内存 DoS,用户自触发)。
- electron-main.js:215 shell:connect 无频率限制。
- 备份文件明文落盘(无凭证;密码设计上从不持久化)。

### F-8【Low·纵深】render.js:247 `sv.innerHTML = U.ICONS[t.key]`(未修改)
- 依赖"ICONS 永不含用户数据"约定;t.key 经 SAFE_ID 约束,当前不可注入。建议未来改为 createElementNS 或加注释固化约定。

### 依赖与外部公告
- ssh2 1.17.0、Electron 43.3.0 均为当前最新;Electron 中危 [CVE-2026-70606](https://github.com/advisories/GHSA-r4w5-6pfg-jxp5)(自定义协议会话缓存)本应用未注册自定义协议,**不适用**。
- 设备页 webview 共用 persist:nettopo-web 分区(设备站间共享 cookie,README 已声明,上轮已接受)。

---

## 3. 正确性 / 性能 / 维护性

| 级别 | 位置 | 问题 | 状态 |
|---|---|---|---|
| 中 | js/model.js:128 | num() 剔除非数字字符后拼接:"1e5"→15、"1 200"→1200、负数坐标回环依赖剥 ' 前缀 | ✅ 已修复(剥 sanitizeCell 前缀 + parseFloat;回归 1e2=100 与坐标回环) |
| 中 | js/layout.js:274,298 | BFS q.shift() O(n²),大图卡顿 | 未改(设计级,低优先级) |
| 低 | js/layout.js:49-51 | 抖动同为零理论除零 → NaN | 未改(概率近零) |
| 低 | js/layout.js:49 | Math.random 抖动致布局不可复现 | 未改 |
| 低 | js/render.js:555,588 | 指针捕获失败无 pointercancel 清理 | 未改 |
| 低 | js/layout.js:105-118 | runLayout 无外部取消句柄 | 未改 |
| 低 | js/vsdx.js:377-378 | 单 `<pp>` 与 VDX 逐行 `<pp>` 不一致(多行标注渲染差异未真机验证) | 未改(需真实 Visio 验证) |
| Info | js/app.js:176/693/1028 | XLSX 缺失提示"需联网"与本地静态副本事实不符 | 未改(文案) |

---

## 4. 正向观察(本轮核验)

- 上一轮全部修复在位且测试覆盖充分(单测从 382 → 385 全绿)。
- 三窗口 contextIsolation + sandbox + 无 nodeIntegration;shell:* IPC 全量 sender 守卫、数据限长、字符串类型校验。
- SSH TOFU:首连暂停握手、SHA256:base64 指纹、**指纹不匹配拒连**、同主机多会话排队;密码仅内存、不落盘、不进日志。
- 备份库:文件名白名单 + Windows 保留名 + 符号链接拒绝 + 64MB 上限 + 临时文件原子改名。
- sanitizeGraph/sanitizeTypeData 白名单清洗(id/坐标/颜色/字体/图片/URL);escHtml 五字符完整转义;normalizeWebUrl + 主进程 isHttpUrl 双重协议白名单。
- webview:popup 双层拦截(主进程 + 渲染层)仅放行 http/https 转标签;证书告警按主机本次运行记忆、窗口关闭即拒绝挂起回调。
- 仓库无硬编码秘密;工作文件(task_plan/findings/progress)已 gitignore;测试基建完善(单元/e2e/双冒烟)。

---

## 5. 本轮改动清单(源码)

| 文件 | 改动 |
|---|---|
| package.json / package-lock.json | 移除 electron-packager devDep 与 `pack` 脚本 |
| index.html | CSP script-src 移除 'unsafe-eval' |
| README.md | 修正 CSP 说明(不再声称 SheetJS 需要 unsafe-eval) |
| electron-main.js | F-3 web:open 主窗 sender 校验;F-4 导航守卫(web-contents-created) |
| js/util.js | F-5 sanitizeCell 前导空白绕过;F-6 escXml 非法控制字符剔除 |
| js/vsdx.js | XRAW 统一 escXml;cell 的 V 值 ATTR() 转义 + extra 约束注释 |
| js/model.js | num() 剥 ' 前缀 + parseFloat(修字符误吞) |
| test/run-tests.js | 新增 3 项回归(控制字符/公式空白绕过/科学计数法坐标) |
| test/_csp-check.js | 新增 CSP 冒烟验证脚本(gitignored) |

**未提交修改的验证结果:** 单测 385/385 ✓ | npm audit 0 ✓ | CSP 冒烟零违规 ✓ | smoke-shell 55/55 ✓(源码版 Electron)

## 6. 建议下一步

1. `build.bat` 重新打包便携版(dist/portable 当前为旧构建,不含 R2 修复)。
2. 补跑 e2e(无头 Chrome)与 smoke-backup 完成验证矩阵。
3. 低优先级(可选):openModal option value 转义、导入大小上限、layout BFS 队列优化、render.js ICONS 注释、vsdx `<pp>` 真机验证。
4. 关注 extract-zip 上游修复与 Electron 新版本,按节奏升级。
