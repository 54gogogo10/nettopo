# NetTopo 安全审查报告 — 2026-09-02

> **修复记录（2026-09-03）**：本报告发现的问题已修复 26 项（Medium 9/9、Low 12/15、加固 4/6），新增 28 项单测（963 → 991 全绿）+ e2e 全段通过。各项修复状态见 §8；未修复项（L2/L5/L14 窗口残留/A4/A6 部分与 M2 的 UI 桥背压）已在 §8 注明原因。

## 1. 范围与方法

- **审查方式**：主进程（electron-main.js / preload.js / 三份 HTML 的 CSP 与窗口配置）人工通读；其余模块 4 路并行专项深审（网络服务 / Shell+监控+备份 / 渲染层 XSS 面 / 导出链+独立窗口），全量通读约 1.95 万行源码，关键结论经代码复现或抽样核验。
- **视角**：与既往 R1–R6 审查（功能正确性）互补，本轮只看安全：不可信输入、注入、路径穿越、凭据、资源耗尽、Electron 加固。
- **威胁模型**：① 局域网内任意主机可连内置 TFTP/FTP/Syslog（无认证或弱认证）；② 恶意 SSH/Telnet/SNMP 设备端（输出、banner、响应均为不可信数据）；③ 恶意工程文件/CSV/Excel（可从他人处导入，含设备名、告警规则、监控配置）；④ 渲染层被 XSS 攻破后的纵深防御（本报告渲染层结论：未发现可利用 XSS，纵深缺口按 Low 记）；⑤ 同机其他低权限用户可读 userData。

## 2. 总体结论

**未发现 High 级问题。** Electron 基线配置良好（三窗口 contextIsolation + sandbox + nodeIntegration:false、宿主窗口导航仅放行 file:、IPC 按窗口校验 sender、证书按主机+指纹固定、下载强制另存为对话框），路径安全三层防线（白名单清洗 → resolve → 前缀/lstat 终判）与渲染层 XSS 防线（单一转义函数 + DOM API 渲染 SVG + 四类白名单 + 入口统一 sanitize）执行一致，均经全量核对无遗漏。

确认 **Medium 级 9 项**（集中在「恶意设备端/局域网客户端消耗主进程资源」与「网络服务协议面」）、**Low 级 15 项**、加固建议 6 项。建议优先修复 M1–M3。

## 3. Medium 级发现

### M1 告警/合规正则 ReDoS 启发式可绕过，可挂死主进程（js/monitor.js:589, 326, 344, 1406）

防护是单一启发式 `/\([^()]*[+*][^()]*\)[+*{]/`，实测 `(a?)+x`、`(a|aa)+b`、`(a|a)*$` 等经典指数回溯模式全部穿过（组内无 `+*` 或只有 `?`/`|`），回溯耗时随输入指数增长。告警模式来自工程文件/任务配置（`opts.alerts`），文本来自设备输出（上限 64KB 只限输入规模、限不了回溯）；主进程**同步**执行 `re.test()`，命中即永久挂起——所有窗口、托盘、IPC 冻结且无法自愈。合规路径同理（`runCompliance` 对最长 8MB 备份逐行扫描）。代码注释自认「非完备防线」。
**修复方向**：正则执行加超时保护（分片/worker）或换线性引擎；启发式仅作第一道过滤保留。

### M2 告警/备份捕获缓冲无字节上限，恶意设备输出可 OOM/灌满磁盘（js/monitor.js:1264, 1399-1401, 1277, 1495, 1575）

`_alertPending` 仅限行数（20000 行），单行可达 ~256KB（lineBuf 256K 断行），join 后才做 64KB 截断——最坏先拼接数 GB 再截断；`_backupCap.lines` 与独立备份会话 `lines.push(t)` 完全无上限，捕获窗口最长约 16 分钟。千兆局域网恶意服务端高速灌输出即可把主进程推到 OOM；`lines.join` 抛 RangeError 时因主进程**没有 uncaughtException/unhandledRejection 兜底**（已核实 electron-main.js 无此二 handler）直接崩溃。日志 32MB 是「滚动新文件」而非总量封顶，一天可写数百个文件灌满磁盘。
**修复方向**：缓冲加字节上限（join 前按行预算、超限丢最旧）；备份输出总量/文件数封顶；主进程补崩溃兜底（见加固 A1）。

### M3 Syslog 日志注入：msg 内嵌换行原样落盘，可伪造审计行（js/svc-syslog.js:96, 321, 381-389）

`parseSyslogMsg` 只剥尾部 `\r\n`、控制字符剔除正则刻意排除 `\u000a/\u000d`，落盘逐条 `write(... + '\n')`——TCP octet 帧/UDP 报文内的原始 `\n` 保留。攻击者一条报文即可在 `<host>/<日期>.log` 注入自带时间戳/级别/主机外观的任意「日志行」，且被 `search()` 原样检索回 UI，无法与真实设备日志区分。本项目面向配置合规巡检场景，日志完整性是实质属性。
**修复方向**：写入前把 msg 内部 `\r\n` 一并替换（与 TFTP 控制字符清洗口径对齐）。

### M4 Syslog TCP 连接数无上限，FD 耗尽可级联停摆服务（js/svc-syslog.js:201-262, 205）

`_onTcpConn` 无并发上限、未设 `srv.maxConnections`，仅靠 5 分钟空闲定时器回收；`srv.on('error')` 一律 `this.stop()`。攻击者批量建连耗尽文件描述符后 accept 产生 EMFILE → server 'error' → 整个 Syslog（UDP+TCP）停摆；FD 耗尽是进程级的，FTP 的 accept 同样 EMFILE，其错误处理也是 `stop()`（svc-ftp.js:574），可波及 FTP。连接数无上限由代码直接可证；EMFILE → stop 的实际触发路径标注**待确认**（验证方法：连接洪泛实测观察 lastError 与 status）。
**修复方向**：设 `maxConnections` 与活跃连接计数上限；server error 仅在 bind 类错误时 stop，EMFILE 降级断开最旧连接。

### M5 Syslog 检索全量 readFileSync，超大日志可致主进程 OOM（js/svc-syslog.js:379-380）

4MB 的 `MAX_LINE_SEARCH` 截断发生在**整文件读入之后**。单日单主机日志文件大小由设备端灌包速度决定（限速 200 条/s × 8KB ≈ 1.6MB/s，数分钟可达数百 MB）；用户在面板执行一次关键字检索即触发主进程全量读入（utf8 字符串再翻倍），内存尖峰可 OOM。检索触发权在用户，文件大小由攻击者控制。
**修复方向**：分块/流式检索，或先 lstat 拒绝超过阈值（如 32MB）的文件并提示。

### M6 FTP PASV 数据连接不校验对端身份，数据通道可被劫持（js/svc-ftp.js:230-238, 245）

PASV 监听 `0.0.0.0`，`connection` 回调把第一个连入者直接当作数据对端，不校验 `remoteAddress` 与控制连接同源。同网段第三方在 PASV 后、真实客户端连入前的窗口期（最长 30s）抢先连入：RETR 场景**窃取设备配置文件**；STOR 场景顶替真实上传者注入伪造配置。用户配置固定 `pasvMin/pasvMax` 时端口高度可预测。
**修复方向**：数据连接建立时校验 `s.remoteAddress === 控制连接对端 IP`，不符即 destroy（标准 FTP 防护）。

### M7 FTP 控制通道无背压、无命令速率限制，命令洪泛可放大内存直至 OOM（js/svc-ftp.js:71-75, 109-119）

每条命令至少回一行，发 2 字节 `A\n` 回约 20 字节错误响应 ≈ 10 倍放大；`sock.write` 不检查返回值，客户端只发不读时服务端用户态写缓冲无界累积；每条命令重置空闲超时可无限维持。无每连接速率/总量限制。
**修复方向**：未决写缓冲超限（`sock.writableLength`）或命令速率超限即断开。

### M8 SNMP 响应不校验来源地址，request-id 全局顺序可预测（js/monitor.js:235, 242-247）

`sock.on('message')` 只校验 rid + community，不校验 `rinfo.address === host`；`snmpGet._rid` 全局从 1 递增、community 明文（默认常为 `public`）。同网段攻击者**盲猜 rid 即可抢答**（嗅探更稳），伪造 sysUpTime → 触发假「设备已重启」告警、伪造 CPU/内存/接口流量与接口 up-down 通知，误导运维。BER 解析本身边界完整，无内存安全问题，属数据完整性问题。
**修复方向**：校验响应来源 IP；rid 混入随机熵。

### M9 FTP 默认弱口令 + 全网卡监听 + 无防爆破（js/net-services.js:25, svc-ftp.js:188, 557; app.js:6570 面板预填）

默认账号 `nettopo/nettopo`，服务开启后面板表单预填且无强制改密提示；监听 `0.0.0.0`；认证失败 5 次仅断开当前连接，重连即再试 5 次，无退避/锁定。得手后可 STOR 植入伪造配置、RETR 下载已收配置、DELE 删除文件。
**修复方向**：面板预填改随机生成口令并提示修改；认证失败加退避/临时封禁；至少在 UI 提示「FTP 为明文协议，口令与配置文件可被同网段嗅探」。

## 4. Low 级发现

| # | 位置 | 问题 |
| --- | --- | --- |
| L1 | net-services.js + electron-main.js:859-865, 855-858 | FTP 服务口令明文写入 settings.json 并原样回传渲染层，与项目「密码经 safeStorage 加密落盘」惯例不一致（SSH 设备密码走 safeStorage，服务口令未走）。得手仅及服务目录，边际影响有限 |
| L2 | monitor.js:1313-1319 | 监控首连「自动信任」主机指纹：TOFU 语义完整（信任→钉扎→变化即拒），但无用户确认（仅事后系统通知），且 monitor-trust.json 无撤销/管理入口；首连瞬间 MITM 会被永久钉扎 |
| L3 | shell.js:58, 85, 173; monitor.js:508 | host/username 无长度与控制字符清洗，内嵌 `\n` 可在会话审计日志/监控日志头注入伪造行（文件名已走 sanitizeFilename，仅内容完整性） |
| L4 | shell.js:189 | 恶意服务端可在输出中回显密码，被「原样留痕」写入本地日志，凭据扩散给日志读取者。建议对输出中与凭据相同的子串做掩码 |
| L5 | shell-ui.js:605 + electron-main.js:511-513 | Web Shell 的 TOFU 确认与指纹记忆均在渲染层：被 XSS 的渲染层可直接调 `shell:trust` 放行任意指纹。架构性纵深边界，monitor 侧主进程信任库不受影响 |
| L6 | config-backup.js:63; monitor.js:857/875 | 写路径（配置备份/日志写流）不查符号链接（读路径均已 lstat 拒绝）；同机攻击者若能**写** userData 可借预置符号链接写任意位置。Windows ACL 默认挡住，纵深备注 |
| L7 | util.js:409-429 | `sanitizeTypeData` 未拦 `__proto__` 键（SAFE_KEY 含下划线），恶意工程 `typeOverrides["__proto__"]` 会触发原型 setter（实测复现）；自定义类型 key 也可取 `'__proto__'`。影响限于颜色/图标串位，无注入，但与 `DANGEROUS_KEYS` 两处既有口径不一致 |
| L8 | app.js:2569, 2793 | 手工编辑设备时 `n.web` 只 `trim()` 不经 `normalizeWebUrl`，`javascript:` 等异常值会被持久化到下次重载才被清洗（打开路径有 `^https?://` 强校验兜底，无 XSS） |
| L9 | app.js:4780 | 工程恢复时监控配置 `alerts` 数组无结构校验直通主进程（恶意工程可塞任意对象/超长串）。建议按 UI 路径同口径白名单重建 |
| L10 | app.js:4568-4569; util.js:469-501 | SNMP 回填 `n.osver` 无长度钳制；`note`/`aIf`/`aIp`/文本框 text 等字符串字段不限长——20MB 工程可携超长文本，渲染转义正确，属存储/布局 DoS 面 |
| L11 | visio.js:140-141/151/206; pdf.js:47-49/59-61/110/115/123/144 | 颜色/描边等属性位插值未本地转义，完全依赖上游 `isValidColor`/固定调色板白名单（当前数据流已验证安全、不可利用）；vsdx.js 同类值有 ATTR 转义纵深，建议对齐 |
| L12 | shell-ui.js:161, 207, 113 | 标签标题（host/设备名可来自恶意 CSV/工程）拼入 `term.write` ANSI 横幅，字面 `\x1b` 被 xterm 解释（改色/改标题/伪造横幅文案）。无脚本执行、无跨标签路径、xterm 无内置 OSC 52，纯装饰性。建议横幅文本过滤 ESC 序列 |
| L13 | svc-ftp.js:26-41, 438; net-services.js:196-203 | `resolveWithin` 不滤控制字符/尾点（与 TFTP/Syslog 口径不一致，Linux 下可建含 CR 文件名并经 LIST 注入裸 CR）；`_resolveFile` 不拒冒号（根目录内 ADS 读/删，增量危害小） |
| L14 | svc-ftp.js:344-347, 381 | `overwrite=false` 的 STOR 存在 TOCTOU（lstat 检查与 rename 之间可被同账号并发连接抢占，单账号设计影响有限） |
| L15 | svc-tftp.js:84, 152-157, 169; electron-main.js:752-814 | TFTP 8 个会话槽可被单主机慢会话占满（饿死其它设备推送，有界可恢复）；伪造 WRQ 可累积空目录；RRQ `readFileSync` 整读（有 32MB×8 兜底）；`monitor:logs-search` 同步扫描最多 300×4MB 阻塞主进程（用户触发） |

## 5. 加固建议（非漏洞）

- **A1 主进程崩溃兜底**：electron-main.js 无 `process.on('uncaughtException'/'unhandledRejection')`。目前任何一个渲染层回调/定时器里的同步异常（如 M2 的 join RangeError、写流 error）都会整体崩溃。建议挂兜底 handler 记录日志并降级，而非裸崩。
- **A2 webview 分区权限收口**：未设 `session.setPermissionRequestHandler`/`setPermissionCheckHandler`，远程设备页可按默认策略请求通知/地理位置/媒体等权限。建议对 `persist:nettopo-web` 分区默认拒绝（fullscreen 可按需放行）。
- **A3 will-attach-webview**：宿主窗口未挂 `will-attach-webview` 清理 guest 的 preload/额外参数。当前 webview 元素仅 src/partition/allowpopups 三属性（已核实），作纵深加固。
- **A4 electron-fuses**：未烧录 fuses（`runAsNode`/`nodeCliInspect` 等默认开启，便携版可被同机用户以环境变量注入方式改变主进程行为）。建议启用 `runAsNode: false` 等。
- **A5 Linux 凭据回退**：无 keyring 环境下 `safeStorage` 可能回退 `basic_text`（弱混淆），工程 JSON 凭据近似明文。建议启动时检测 `safeStorage.getSelectedStorageBackend()` 并在 UI 提示（待确认项：需无 keyring 的 Linux 实测）。
- **A6 文档提示**：DPAPI 密文强度与 Windows 登录口令绑定（弱开机口令机器上泄露的 .nettopo 可离线爆破）；Telnet/FTP/SNMP v2c 均为明文协议。建议在 README/帮助中说明适用边界。

## 6. 已检查且确认安全（要点）

- **Electron 基线**：三窗口 contextIsolation+sandbox+no nodeIntegration；宿主窗口 will-navigate 仅放行 file:、window.open 全拒（防 preload 桥泄露）；webview guest 的 window.open 双层 handler 只放行 http(s)；证书按「主机+指纹」记忆、变化重新询问；双分区 will-download 强制另存为；IPC 全部按窗口校验 sender；`shell:data` 限字符串 1MB；测试钩子 `monitor:test-close` 有环境变量门控。
- **路径安全**：TFTP/FTP/Syslog/monitor/config-backup/backup-store 六处均为「白名单清洗 → path.resolve → 前缀终判」三层；lstat 拒符号链接（读路径全覆盖）；保留设备名、NTFS ADS（TFTP 侧）、控制字符、尾点空格均有处理；未发现清洗后二次拼接复活穿越。
- **渲染层 XSS**：114 处 innerHTML 全量核对——不可信数据出口均经 `escHtml`（转义顺序正确、五实体全覆盖）或 textContent/DOM API；SVG 画布全用 createElementNS；图片/颜色/字体/id 四类白名单；三条不可信入口（表格/工程/备份/localStorage）统一收敛到 sanitize 系列函数；日志任意字节、告警 matchedText、TFTP 文件名、syslog 消息等高危出口逐一验证已转义；无 javascript:/data: 可点击注入点。
- **导出链**：VDX/VSDX 所有 XML 拼接点经 escXml（含属性上下文与控制字符剔除）；VSDX 纯内存生成无 zip-slip、entry name 无用户数据；图片 dataURL 白名单；下载文件名全为固定模板+日期；`U.zipFiles` 条目名过滤到位。
- **凭据与协议**：日志头只记用户名不记密码；monitor 断连即清凭据副本；指纹变化即拒连语义完整（monitor 与 Web Shell 双路径）；ICMP 探测 `spawn('ping', [...])` 数组参数无 shell 注入；SNMP BER/TFTP/FTP/Syslog 解析边界校验完整无越界；FTP PORT 反弹已封（目标钉死为客户端自身）；端口配置 clampPort 类型安全。

## 7. 修复优先级建议

1. **立即**：M1（ReDoS）、M2（缓冲上限 + A1 崩溃兜底）——恶意设备端即可触发且后果是主进程挂死/崩溃，影响所有功能。
2. **短期**：M3（日志注入，合规场景硬伤）、M6（PASV 对端校验，标准 FTP 防护缺失）、M8（SNMP 来源校验）、M9（弱口令预填）。
3. **随迭代**：M4/M5/M7（资源上限类）、L1/L3/L7/L9/L12、A2–A4。

## 8. 修复记录（2026-09-03）

**验证**：单测 963 → 991 全绿（新增 28 项安全回归，见 test/run-tests.js「安全修复回归」节）；e2e 34 段两遍全绿（无控制台错误）；全部改动文件 node --check 通过。

### Medium（9/9 全修）

| # | 修复方式 |
| --- | --- |
| M1 | 新增 `js/regex-lab.js` + `js/regex-lab-worker.js`：告警/合规正则在独立工作线程内执行（默认 5s 超时，合规 15s），超时处决→拉黑该模式（进程内永久不命中）→按 begin 报号定位元凶后重启线程续跑其余规则；主进程事件循环永不阻塞。静态启发式同步扩展（`(a?)+`/`(a|aa)*` 等组内含 `?`/`\|`/`{` 的被量词化组一律拒绝，monitor 与渲染层 cleanComplianceRules 同口径）。被拉黑规则记入监控日志提示修改 |
| M2 | 告警待检缓冲加 1MB 字节上限（丢最旧）；`_checkAlerts` 改为先按字节预算截取再 join（64KB 上限语义不变，join 前内存有界）；备份捕获（共享/独立会话两路）加 8MB 字节封顶（与 config-backup 单份上限对齐），超限记日志；滚动日志文件数按设备按日封顶 24 个（`_pruneLogFiles`，lstat 拒符号链接）。**未做**：shell.on('output')→webContents.send 的渲染层背压（渲染层冻结时队列在 Electron 内部，无可行测点），风险维持原评估 |
| M3 | `parseSyslogMsg` 把 msg 内嵌 CR/LF 折叠为空格（保留剥尾部换行语义），日志行注入不再可能；单测覆盖 |
| M4 | Syslog TCP `srv.maxConnections = 64`（内核层挂起超限 accept）；UDP/TCP server 的 error 处理区分致命（EACCES/EADDRINUSE 等 stop）与暂时性（EMFILE/ENFILE 仅记错误，等空闲超时回收自愈）；FTP 同口径（maxConnections = maxSessions+16 + EMFILE 非致命） |
| M5 | syslog `search()` 改为只读尾部 MAX_LINE_SEARCH(4MB) 字节（fd 读取 + try/finally 关闭，丢弃首个残缺半行），语义与原「读全量再 slice 尾部」一致；4.5MB 大文件检索单测覆盖 |
| M6 | PASV 数据连接建立时校验 `remoteAddress` 与控制连接对端一致（`::ffff:` 归一化），不符销毁并继续等待真实对端直至超时 |
| M7 | 控制通道每命令检查 `sock.writableLength`（>1MB 断开）+ 每连接命令速率 300/s 上限，洪泛即断不回复 |
| M8 | `snmpGet` 响应校验来源地址（host 为点分 IPv4 时 `rinfo.address === host`）；request-id 混入进程级随机盐（盐<<15 \| 顺序序号）；正向对照+伪造抢答（合法 rid/community 但错误来源）单测覆盖 |
| M9 | 面板「保存并应用」时若开启 FTP 仍为默认口令 nettopo（或空）→ 自动生成 16 位随机口令并回填表单+toast 提示；面板补「FTP 为明文协议，勿复用高权限口令」提示；服务端新增来源 IP 维度防爆破：10 分钟窗口内认证失败 15 次封禁 10 分钟（封禁表上限 500，到期自动解除） |

### Low（12/15 修复）

- **L1 ✅** settings.json 中 FTP 口令经 safeStorage 加密（`enc1:` 前缀标记），恢复启动时解密；加密不可用回退原值（旧版兼容）；`netsvc:get` 回运行态明文供面板回填（内存内，不落盘）。
- **L3 ✅** shell.js/monitor.js 建连入口剔除 host/username/跳板字段的控制字符（审计日志头无法注入伪造行）。
- **L4 ✅** 凭据打码：shell 审计日志写前替换（密码/私钥口令/跳板密码，≥3 字符）；monitor 日志行打码，并覆盖告警缓冲与备份捕获（共享/独立两路），恶意服务端回显密码不再落入任何落盘文件。
- **L6 ✅** 写路径补符号链接检查：monitor `_openLog`（设备目录）、config-backup `save`（设备/主机目录）、syslog `_writeEntry`（主机目录）——存在符号链接即拒绝写入。
- **L7 ✅** `sanitizeTypeData` 拒收 `__proto__/constructor/prototype` 键（typeOverrides 与 customTypes key 两处），与 DANGEROUS_KEYS 既有口径对齐。
- **L8 ✅** 设备编辑/新增保存时 `n.web` 统一过 `normalizeWebUrl`（异常值不再被持久化）。
- **L9 ✅** 工程恢复时监控 `alerts` 白名单重建（仅字符串或 {pattern,note}，各限 300 字符、最多 32 条）。
- **L10 ✅** SNMP 回填 osver 钳 64 字符；sanitizeGraph 补 note(2000)/连线接口与 IP(64)/bw(32)/连线备注(500)/文本框(10000) 限长。
- **L11 ✅** visio.js/pdf.js 颜色/描边属性位补本地转义（对齐 vsdx.js 的纵深防御口径）。
- **L12 ✅** shell-ui 终端横幅插值统一经 `bannerText`（剔除 ESC 与 C1 控制符），ANSI 注入封堵；顺带 escAttr 补单引号实体（L17，webview-ui 同）。
- **L13 ✅** FTP `resolveWithin` 拒控制字符、剥尾点/尾空格；net-services `_resolveFile` 拒冒号（NTFS ADS）。
- **L15 ✅** TFTP 新增单来源 IP 会话配额（默认 4，可配）；中止会话后空目录顺手清理。**保留**：logs-search 同步扫描（用户触发、有 300×4MB 上限）、RRQ 整读（有 32MB×8 兜底）。
- **L2 / L5 / L14 未修**：L2（监控首连 TOFU 自动信任）与 L5（Web Shell 指纹确认在渲染层）为架构取舍，语义本身完整（钉扎+变化即拒），维持现状；L14 TOCTOU 已加 rename 前复核（窗口缩至毫秒级，彻底消除需 OS 级原子语义，维持 Low）。

### 加固（4/6 落地）

- **A1 ✅** 主进程挂 `uncaughtException`/`unhandledRejection` 兜底：记录到 userData/main-crash.log 后降级继续运行（不再整体崩溃）。
- **A2 ✅** `persist:nettopo-web` 分区设置 `setPermissionRequestHandler`/`setPermissionCheckHandler`：默认拒绝，仅放行 fullscreen。
- **A3 ✅** `will-attach-webview` 兜底：删除 guest preload、强制无 Node、src 非 http(s) 一律 preventDefault。
- **A5 ✅** Linux 启动时检测 `safeStorage.getSelectedStorageBackend() === 'basic_text'` 弹系统通知提示凭据保护降级。
- **A6 部分 ✅** README「说明」补充安全边界段（DPAPI/密钥环强度、明文协议、内置服务防护与正则限时执行的说明）。
- **A4 未做**：electron-fuses 烧录需构建链改配与产物验证（afterPack 钩子），单独迭代，见待办。

### 新增单测（28 项，test/run-tests.js「安全修复回归」节）

RegexLab 超时处决/拉黑/scan ×5、扩展启发式 ×1、监控凭据打码 ×2、syslog 换行折叠 ×1、大文件尾部检索 ×1、FTP 封禁 ×3、FTP 路径清洗 ×3、TFTP 每 IP 配额 ×3、SNMP 来源校验 ×2、Shell host 清洗 ×1、Shell 日志密码打码 ×1、类型清洗原型键 ×2、工程字段限长 ×3。
