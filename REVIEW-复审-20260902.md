# NetTopo 全面代码审查与修复（R6）— 2026-09-02

## 1. 范围与结论

- **审查方式**：7 路并行专项审查（渲染核心 / 画布交互 / util+model / 导出链 / 主进程+Shell / 网络服务+监控备份 / 独立窗口 UI），全量通读约 2 万行源码，交叉验证调用契约。
- **结论**：确认问题约 45 项（High 9 / Medium 22 / Low 14），本次修复 **42 项**；3 项涉及表格导出格式设计变更，列为待办单独评审（见 §4）。
- **验证**：单元测试 946 → **963 全绿**（新增 17 项）；e2e 34 段两遍全绿（无控制台错误）；Electron 冒烟 smoke-services / smoke-monitor / smoke-backup / smoke-center 全部通过。

## 2. High 级修复（9 项）

| # | 位置 | 问题与修复 |
| --- | --- | --- |
| H1 | js/shell.js | 会话审计日志写流未挂 error 监听：磁盘满/文件被锁时异步 error 事件直接崩主进程。新增 `_makeLogStream` 统一创建并挂监听静默降级（开文件与滚动两处） |
| H2 | js/svc-ftp.js | FTP STOR 写流同样无 error 监听（崩溃）；且 `ws.end` 回调在流出错时仍触发，把半截文件 rename 成成品并回 226。补 error 监听 + failed 门卫 |
| H3 | js/svc-syslog.js | RFC5424 数字时区偏移符号反了（`+08:00` 应减偏移却加了，时间差 2×offset，归档/检索错天）。修正符号，新增 ±偏移单测 |
| H4 | js/monitor.js | `OID_IF_SPEED` 误用 ifAdminStatus(.7)（枚举 1/2/3），接口速率采集全错。改为 ifSpeed(.5) |
| H5 | js/app.js restore() | 跨页撤销时 `sheetStash()` 在数据替换之后执行，当前页被他页快照覆盖（两页同化丢数据）。调整为先暂存当前页再替换 |
| H6 | js/render.js | 多选拖拽 dx/dy 相对「被抓取节点」，偏移基准却取 `ids[0]`：抓非首个选中节点时整组瞬移并持久化错误坐标。基准统一为被抓取节点 |
| H7 | js/model.js | 导出列「VLAN接口」无导入角色映射，SVI 数据导出后无法回读（整列丢失、导入分支死代码）。ROLE_SETS 补 vlans 角色，导出串支持 `id:ip/mask`，新增往返单测 |
| H8 | js/vsdx.js + js/visio.js | Shape ID 分段方案（线 id+10000/图片 30000/图例 40000/文本 50000）在链路 ≥ 10001 时区间重叠产生重复 ID（Visio 拒开/丢形状，实测复现）。全部形状改同一计数器顺序分配，新增万级链路唯一性单测 |
| H9 | js/render.js zoomBy | 默认锚点多减 r.left/r.top，按钮/键盘缩放每次漂移约 (k−1)×r.top。修正为画布中心本地坐标 |

## 3. Medium / Low 级修复（33 项，摘要）

**主进程与网络服务**
- 会话归属标记（owner）：Web Shell 窗口关闭改为 `closeAll('monitor')`，不再误杀后台监控/备份连接；监控侧会话 end 即清 `_params`，7×24 重连循环不再无界累积凭据副本。
- SSH keepalive（15s）：NAT 静默掐断的半开连接从「等十几分钟内核超时」变为约 1 分钟可感知断开。
- 单实例锁（requestSingleInstanceLock）：杜绝双开导致的端口互抢/设置互相覆盖/双托盘。
- 托盘创建失败（图标缺失/无托盘的桌面环境）时按「托盘不可用」处理，关窗即退出，不再隐藏成无窗无盘僵尸进程。
- `reconnect` 加会话存活门控（防同 sid 双会话互杀）；Telnet 子协商无 IAC SE 时滞留缓冲设 64KB 上限。
- 日志检索 readSync 抛错泄漏 fd（try/finally 关闭）；safeStorage 解密失败返回空串显式判失败（空密码无限重试误导排障）。
- Syslog TCP framing：数字开头的换行消息被误判 octet 帧头致整条连接死锁——帧未收全期间等来换行则按换行 framing 处理；空闲超时改为覆盖整个连接生命周期。
- TFTP WRQ 收尾 rename 失败（目标被占用）如实回 ERROR、不广播收件、保留 .part 供找回；会话校验 TID 补端口维度；文件名清洗补 `:`（NTFS 交替数据流）。
- FTP PORT 目标须与控制连接同源（防反弹）；数据传输期间刷新空闲超时（慢速大文件不再被 421 掐断）。
- net-services：start 失败回滚 applied 标记（同端口配置不再被短路到重启）；electron-main 恢复配置补 `.catch`。
- monitor：`_onOutput` 不再因日志流降级整体停摆（仅读取任务此前永久丢输出/告警失明）；日志跨天滚动并入 `_logLine` 写前自查（仅读取/仅探测任务不再写旧日期目录）；独立备份连接失败路径补 close；命令循环改按轮次起点调度（周期不再正漂移）。

**渲染层与 UI**
- removeSheet：删除前序页不清撤销栈（跨页撤销跳错页）→ 清栈；删除当前页未切 regions（区域串页并被持久化）→ 补切；清栈后同步撤销按钮。
- 一批变更操作（编辑/删除设备、连线增删改、批量编辑、类型删除重派）变更后补 `saveGraph()`，直接关窗/刷新不再丢最后一次操作。
- `deleteNode` 把被删设备的监控配置随撤销快照带走，Ctrl+Z 连配置（含加密口令）一并恢复。
- `reconcileMonitors` 的 sysinfo 载荷补齐 sysUpTime/perf：软件重启/恢复工程后重启检测与 CPU/内存采集不再静默失效。
- `nodeBySvcIp` 接口 IP 匹配分支访问不存在的 `l.a.node`（永不命中）→ 改 `l.aIp/l.bIp`；网络服务面板按接口 IP 也能匹配设备。
- 浏览器版后台对齐路径（reconcile/sync/stopForNode）静默降级，不再每次刷新/导入/切页/删设备弹「需要桌面版」（用户主动打开监控中心仍提示，e2e 口径不变）。
- preload 全部 onX 订阅返回退订函数；监控中心/网络服务面板关闭时注销全部 ipc 订阅（长会话不再随开合次数累积监听器）。
- openHelp/openAbout 改为插入正文节点，不再整体替换 form 内容丢「知道了/关闭」按钮。
- 裸字母快捷键（L/F）排除 Ctrl/Meta/Alt（Ctrl+L 不再触发不可撤销的自动布局；自动布局本身也补了撤销快照）；文本框拖拽入撤销栈；自动备份变化检测 hash 补 regions/sheets；剪贴板复制改 Promise 语义（失败不再谎报已复制）。
- 平移/框选补 pointer capture 与 pointercancel（拖出窗口松开不再泄漏监听器/残留框选矩形/卡平移态）；右键不再触发拖拽；flash('link') 补连线高亮 CSS；`--fg/--bg2` 未定义变量改为真实主题变量。
- VSDX 直角连线模式透传 ortho（导出与画布一致，折线按折点生成多段几何）；带宽图例改底部横排（不再上叠设备/溢出页顶）；PDF/PNG/VSDX 超长设备名与接口名按 40 字符收敛；PDF/PNG 导出 canvas 尺寸上限（12000px，超限自动降 scale）+ 空数据流检测（不再产 0 字节图片的“成功”PDF）。
- Web Shell：并发首连的第二个指纹确认框排队展示（不再静默丢弃致 12s 超时）；断线重连横幅被 `> div` 选择器撑成全高遮挡终端 → 终端容器独立类名；浏览器直接打开 shell/webview 页显示「需要桌面版」提示（不再死白页）；设备管理页地址栏对 HTTP 明文页加琥珀色不安全标识。

**导入导出与数据清洗**
- 「IP1/IP2」「端口1/端口2」表头识别（数字后缀按端归属：1→源、2→目标；此前 ip1/ip2 一律算源端、端口 1/2 不识别，接口 IP 静默丢列）。
- 公式注入 `'` 前缀导入统一剥除（导出→导入幂等，设备名 `-SW1` 不再变 `'-SW1`）。
- 网段校验按两端一致掩码计算（/23 合法互联不再误报，与 checkConfigs 口径一致）；悬空链路引用防崩（validateTopology/shortestPath）；typeOf 的 rt 加词边界（PortChannel1/support 不再误判路由器）；sanitizeGraph 节点/文本框宽高与字号补上限钳制；detectDelim 按出现次数计票；buildCSV 加引号判定包含自定义分隔符。

## 4. 待办（未修，需设计评审）

- **表格导出格式扩展**：单链路两端各有管理地址时 b 端数据无法在一行内承载；设备备注（n.note）无独立导出列且导入会被链路备注占用。两者都需扩列/扩行格式变更（影响 README 表格契约与旧版兼容），建议单独迭代。
- VSDX 对 GIF/WebP 图标的转码（当前白名单允许上传但导出时静默丢弃）；标注防碰撞无画布边界钳制（密集拓扑标注可能被推出可见区）——Low，随迭代处理。

## 5. 测试

- 新增单测 17 项：syslog ±时区偏移、VLAN 接口列往返（含掩码）、IP1/IP2 与端口 1/2 表头、公式前缀幂等、/23 不误报、悬空链路防崩 ×2、typeOf 词边界 ×3、buildCSV 自定义分隔符转义、detectDelim 计票、VSDX 万级链路 Shape ID 唯一性；1 项断言随 Shape ID 方案变更更新（文本框不再绑定固定 ID 段）。
- smoke-shell 未跑（本机历史环境问题，见 R5 §4；本次 shell.js 改动经 963 项单测中的 SSH/Telnet 协议用例覆盖）。
